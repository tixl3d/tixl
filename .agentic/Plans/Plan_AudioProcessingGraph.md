# Audio processing graph — clips as reference-emitting operators

Supersedes the "audio clips as data" direction of [`Plan_TimelineAudioClips.md`](Plan_TimelineAudioClips.md). That plan's Phases A–C (the `TimelineAudioClip` payload shape, native-rate playback, N-clip engine) are **kept and reused**; its Phases D–F (rebuild a parallel non-op editing/render path, delete the `[AudioClip]` op) are **cancelled** — this plan goes the other way and makes the op canonical.

## Goal

One audio model: **every timeline audio clip is a real operator.** No data-only clip type, no permanent second system. Audio becomes consistent with video (`[VideoClip]`) and data (`[LoadDataClip]`/`[DataClipPlayer]`), so a new user never asks "where are the AudioClip ops?" and gets "it's complicated" as the answer.

The mechanism generalises TiXL's existing **field / shader graph** (`ShaderGraphNode`): clip/mix/group operators emit a lightweight *reference node* on the wire that declares structure and animatable parameters; a downstream **root** traverses that graph and *realises* it against the BASS mixer. The reference/dataflow/direction distinction is invisible to users — they just wire ops together.

Three usage tiers, all on one substrate:

1. **Fast & dirty** — clips auto-play, no wiring. Traditional main soundtrack. (implicit collection)
2. **Working** — a clip with AutoPlay off and nothing collecting it is silently placed. (opt-in)
3. **Full control** — route/group/mix/duck through operators, keyframe any parameter. (explicit reference graph)

## Implementation status (resume here — updated 2026-07-31, G6-lite cleanup landed)

**State: G1–G4 complete and user-tested on real hardware; spikes retired + taxonomy cleanup done.** The real ops exist and work: `[CombineAudio]` (combinator), `[AudioBus]` (root/reconciler), `[AudioClip]` graph integration. All work is **uncommitted on branch `feat/refactor-audio-processing`** (user commits). Core + Lib build clean.

Spike validation (R1 collection / R2 live BASS routing / join + animated params) all ✅ — details in git history and the G-step notes below.

**G4b — canonical `[AudioClip]` in the graph (the big integration):** new `AudioReference` output (`Slot<AudioGraphNode>`, GUID `4c9e7a20-3f81-4d5a-b6e2-1a2b3c4d5e6f`). Architecture: *reuse the soundtrack path* — `AudioEngine`/`SoundtrackClipStream` keep creating + position-syncing the stream (per-channel BASS ops, mixer-agnostic); when wired into a bus the graph owns only **mixer membership + gain**. See the G4b bullet under "Graduation steps" for the full mechanism + remaining caveats.

**Hardening from live testing (all user-verified scenarios):**
1. **Bus-as-heartbeat** — `AudioClip.UpdateAudioReference` calls `AudioClipCollector.RegisterIfActive`, so wiring a clip into a bus plays it (no AutoPlay/player needed).
2. **Membership-based reclaim** — un-routing a clip re-adds its channel to the SoundtrackMixer via a per-frame `ChannelGetMixer()==0` check (a one-shot transition raced the bus's removal and left the channel mixerless = muted).
3. **Stale-bus gating** — a bus that stops being evaluated pauses its submix (was: frozen last state, deaf to params). `Core/Audio/AudioBusRegistry.cs` (heartbeat + sweep from `AudioEngine.CompleteFrame`, 2-frame slack).
4. **Transport gating** — `PlaybackSpeed == 0` pauses all graph submixes (explicit buses + the loose-source default bus); paused mixers stop pulling generator streams, so tone synthesis truly halts. Matches soundtrack-clip behaviour.

**Files touched (uncommitted):** `Core/DataTypes/AudioGraphNode.cs` (+`ExternallyManagedChannel`), `Core/Audio/{AudioEngine,SoundtrackClipStream,TimelineAudioClip,AudioGraphCollector}.cs`, `Core/Audio/AudioBusRegistry.cs` (new), `Operators/Lib/io/audio/{AudioClip,AudioBus}.{cs,t3ui}`. (`_AudioFileSpike.{cs,t3,t3ui}` — throwaway G4a file source — is already committed.)

**G6-lite — spikes retired + taxonomy cleanup (done 2026-07-31):**
- All four spikes deleted (`_AudioSourceSpike`/`_AudioGroupSpike`/`_AudioRootSpike`/`_AudioFileSpike` + `.t3`/`.t3ui`); `_AudioFileSpike` was not graduated — a loose looping file source can come later as `[PlayAudioFile]` if wanted.
- `[AudioToneGenerator]` rebuilt as a real graph source: unrouted decode stream on a new `AudioReference` output (`7c8f3a2e-0001-4e1f-8a5c-6b2d9f7e4c3a`, `DirtyFlagTrigger.Always` like `[AudioClip]`), volume/mute moved onto `AudioGraphNode.Gain` so bus/group folds compose (the stream renders waveform × ADSR only). Kept: waveforms, ADSR trigger/gate, `IsPlaying`/`GetLevel`/`Result` outputs. Fixed broken defaults (Volume 0→0.8, sensible ADSR, Sine default). Param application is frame-keyed so multi-output evaluation doesn't double-run trigger edge detection.
- **Decision (2026-07-31, live test): a tone generator is graph-only — no loose auto-play.** It deliberately does *not* implement `IAudioSource`: unlike `[AudioClip]` (which sounds by being on the timeline), it has no AutoPlay and no meaningful use without animated parameters, so an unwired instance stays silent and breaking its connection silences it immediately (was: the release tail handed off to the implicit default bus — heard as a glitch). After an evaluation gap the envelope resets instead of firing a phantom trigger edge on rewire. Narrows open question 11: implicit audibility is for timeline-presence sources, not procedural ones.
- Renames (GUID-safe): `AudioPlayer` → `[PlayAudioSample]`, `SpatialAudioPlayer` → `[PlaySpatialAudioSample]` (files, classes, `.t3`/`.t3ui` name comments, AudioPlaybackExample annotations). `SpatialAudioPlayerGizmo` / `GetAllSpatialAudioPlayers` keep their names.
- `[PlayAudioClip]` hard-deleted; its one in-repo instance (VJDeadlineAnimals) removed; the Audio asset type's graph drop-op now points at `[PlayAudioSample]` (`AssetHandling.cs`).

**G5 metered-float path — `[AudioLevel]` + `[Duck]` landed 2026-07-31 (needs live test):**
- `[AudioLevel]` — pass-through tap (combinator node, gain 1) + a `Level` float output (`DirtyFlagTrigger.Always`): collects its leaves and meters each channel via `BassMix.ChannelGetLevel` (non-destructive; only meters while a buffered mixer is pulling the channel — unrouted or unbuffered/externally-managed channels report −1 and are skipped), takes the max × collected gain. Insert between sources and a bus, or branch into it.
- `[DuckAudioLevel]` (né `Duck`) — level → smoothed gain (float-only op, keeps the reference graph a pure tree per §Ducking): reduction ramps from `Threshold` to full scale × `Amount`, response curve shaped by a `GainAndBias` knee; exponential smoothing with separate Attack (duck) / Release (recover) time constants on `Playback.RunTimeInSecs`. Wire its `Gain` into a `[CombineAudio]`/`[AudioBus]` Volume. User-tested ✅.
- `[AudioBus]` `Level` output ✅ — meters the whole submix via `BassMix.ChannelGetLevel` (post source gains, pre master Volume): the "react to what you hear" tap the FFT-retirement §3 default wants.
**G5 FX inserts — `[AudioReverb]` + group-as-submix landed 2026-07-31 (needs live test):**
- Wire shape (additive): `AudioGraphNode.FxInsert` — Apply/UpdateParams/Remove callbacks, all taking the realised submix handle, so the wire stays BASS-agnostic and the declaring op owns the effect specifics. `Collect` now tags each source with its **outermost** FX ancestor (`CollectedSource.FxNode`; nested inserts inside inserts are not realised yet).
- `[AudioBus]` realises one nested submix per collected FX node (group-as-submix), reconciles per-channel *target* mixers (`channel → mixer` maps, so rerouting dry↔wet is one remove+add), calls `UpdateParams` every frame, and retires vanished FX groups with a 400 ms gain fade + deferred free so effect tails ring out instead of truncating.
- `[AudioReverb]` — first FX op (DX8 reverb, decided over BASS_FX for zero extra dependencies; backend swappable later without touching the wire shape): combinator with `Volume` fold plus `Mix` (0..1 → dB wet mix), `Time` (decay seconds), `HighFreqRatio`. Per-submix fx-handle dict so a node realised by two buses (multi-send) works.
- Perf note (discussed 2026-07-31): don't gate bus reconciliation on the composition's `VersionCounter` — channel handles change without structural edits (clip streams follow the playhead, lazy stream creation, hot reload) and the graph can span composition boundaries; the per-frame diff is what self-heals those. If reconciliation ever profiles hot, the designated lever is `StructureHash`/`ChangedFlags` computed in the traversal (incl. channel handles), per the ShaderGraphNode pattern.
- **FX chains landed (2026-08-01, found in live test — echo-into-reverb only realised the outer insert):** `Collect` now tags each source with its *nearest* FX ancestor and reports the FX nesting as `FxEdge(fx, parent)` pairs (parent-before-child order); `[AudioBus]` realises one nested submix per FX node with its parent being the enclosing FX group's submix (or the bus), re-parenting on rewire. Chained inserts now stack; a retired outer group's inner submixes self-heal to a new parent next frame.
- **Timeline soundtrack lookup rewritten (same session):** `PlaybackUtils.TryFindingSoundtrack` walks the composition path asking each level's `TryGetMainSoundtrack` directly (settings list + op union) instead of first hunting an `Enabled` settings owner — a `Display = BackgroundImage` clip now shows as the timeline background wherever it lives, without project settings. Note: the clip block also stays visible in the layer grid (deliberate for now — it's the selection/drag handle; revisit with the Figma pass).
- **ManagedBass DX8 EffectType enum is misnumbered (found 2026-08-01 via identity-write probing):** native BASS order is Chorus=0, Compressor=1, Distortion=2, Echo=3, Flanger=4…, but ManagedBass omits Compressor from slot 1 and appends it at 4 — so `EffectType.DXEcho`(2) creates a native *Distortion* and `DXCompressor`(4) a *Flanger*; only `DXReverb`(8) happens to be correct. `[AudioEcho]` (`(EffectType)3`) and `[AudioCompressor]` (`(EffectType)1`) now use named native constants; `[AudioEcho]` also pushes params as a raw native struct. Rule for future DX8 FX ops: **never trust `EffectType.DX*` — use the native constant.** (The diagnosis path — FXGetParameters read-back showing distortion defaults, per-field range probing — is worth remembering for effect mysteries.)
- **`[AudioLevel]` usability follow-up:** a side-branch tap on an `[AudioClip]` silently reads 0 (clip channels are unmeterable outside a realised submix) — user-tested confirmed the inline wiring works. The op should hint at the misconfiguration: `IStatusProvider` warning ("wire this tap inline to meter timeline clips") when the side-branch fallback collects an externally-managed leaf. Cheap now, prevents the trap the user walked into twice.
- **`[AudioLevel]` re-based on submix metering (2026-08-01, found via voice-over ducking):** per-leaf metering couldn't read graph-routed `[AudioClip]` channels (un-buffered, unmeterable) — the canonical duck-music-under-VO case. The tap now declares an *effect-less* `FxInsert`, so the routing bus realises it as its own buffered submix (existing FX-group machinery, zero new realiser code) and `Level` meters that submix — works for every source type, post source-gains. Per-leaf gain folding is gone; the submix sum is what's measured.
- **Timeline file-drop fixes (2026-08-01):** (a) `DragAndDropHandling` gained rect-based drop zones (`TryHandleDropOnRect`) and the timeline now accepts clip drops across its whole window, not just the drawn layer rows (drop layer clamped near existing rows); (b) an external drop no zone claimed used to stay *armed indefinitely* and fire later at whatever drop zone the mouse next crossed — usually the graph, which then created the asset's Primary op (`[PlayAudioSample]`) instead of a timeline clip. Unclaimed drops now expire after one frame.
- **Settings-window soundtrack section fixed for op-provided soundtracks (2026-08-01):** the panel edited `handle.Clip` fields directly — no-ops/silent reverts for an op clip (re-synced from op inputs each frame). Now it detects `handle.Owner is IAudioClipProvider`: *Remove* sets the op's `Display` back to `Clip` (un-designates; op + audio stay), *path edits* go to the op's `Path` input (both undoable via `ChangeInputValueCommand`), *Offset* moves the op's own `TimeClip` (whole clip, length preserved). Still legacy: the **"Add soundtrack to composition" button creates a settings-list entry** — per §Retire it should create an `[AudioClip]` op (one action: pick file → op with AutoPlay + BackgroundImage). Small follow-up.
- **Feature batch 2026-08-01 (user requests):** ✅ `[AudioClip]` `Loop` input — repeats the source window for the clip's timeline length (wrap in `UpdateSoundtrackTime` + export-mixer path; `SourceDurationSecs` now synced from `SourceRange`, so end-trims bound source audio properly; body renderer tiles the waveform per repeat with border lines). ✅ Audio clips use `UiColors.ColorForAudioGraph` in the timeline instead of the per-clip random hue. ✅ select-following-clips (`Ctrl+Shift+A` = mouse-time anchor, context menu = playback-time anchor, all layers). ✅ Waveform = single peak shape (decision); VolumeLevel normalized to the file's max RMS. ✅ Clip-timing popup missing-PopID fix. ✅ Negative source time stays muted (engine + export); loop window clamps to file content (fixes stretched-instead-of-tiled loop images after end-trims, which extend SourceRange past the file). ✅ Trim footage-extent outline + boundary snapping now also for audio clips (was video-only; same duration-cache pattern). ✅ Timeline media-clip styling pass: loop lines `BackgroundFull.Fade(0.6)` spanning the full body (gap fixed), audio *and* video clips use their type colors (`ColorForAudioGraph` / `ColorForTextures`) instead of random hues, media clip bodies render translucent and solidify on hover (links body ↔ footage-extent outline). ✅ Follow-playhead scrolling (`UserSettings.TimelineFollowsPlayback`, default on, Settings→Timeline; scrolls when the marker crosses the 20% edge padding in playback direction). ✅ Split-clip reconnect fixed — it only copied *TimeClip-output* connections into multi-inputs; now all outputs (an `[AudioClip]`'s AudioReference into a bus reconnects too). ✅ `AutoCollectClips` on **both `[AudioBus]` and `[CombineAudio]`** (shared `LooseAudioClipScanner`, Lib): routes unwired sibling `[AudioClip]`s implicitly — on a combine they join its node's inputs, so group volume, `[AudioLevel]` taps and FX ancestors apply (the ducking use case: auto-collect VO clips into the tapped group). Evaluation of the clips' reference outputs doubles as heartbeat; new `AudioGraphNode.LastCollectedFrame` stamp lets a clip flag itself graph-routed without a wire (`RefreshGraphRouting` = wired OR recently collected, 2-frame slack). Only one auto-collecting op per composition (documented, not enforced). Queued: **full audio/video clip-rendering alignment** (shared body-renderer structure; video thumbnails-in-clip would mirror the audio waveform — larger refactor, do when the timeline gets its dedicated pass). **Hover-scrub / FCP-style skimming** (idea 2026-08-02): a skim playhead under the mouse would give timeline actions a *visible* mouse-side anchor — select-following etc. could anchor to skim-position-when-skimming, playhead-otherwise (the invisible mouse anchor was what made the first select-following design confusing). Same timeline pass. Deferred: waveform image resolution for 10min+ files caps at the 16k texture limit (~25 px/s at 5 min, halving from there) — would need segmented/stitched images or an on-GPU renderer; revisit with the `Precise` style (§AudioClip display).
- Still open in G5: `Direct`/spatial realization, multi-send split streams, FFT-as-graph-tap (→ "Retire IsMainSoundtrack" §3). **Requested (2026-08-01): graph-integrate `[PlayAudioSample]`** — emit an `AudioReference` by default so the sampler routes through buses/FX/duck like `[AudioClip]` (needs: engine channel exposure, routed-mode volume ownership, membership reclaim — mirror the G4b clip pattern).
- Bus-to-bus handoff fixed (2026-08-01, found pinning a second bus): `MixerAddChannel` fails silently when the channel still sits in *another bus's* submix — the reconciler only pre-removed for externally-managed/self-tracked channels. Now it removes from whatever mixer holds the channel (`ChannelGetMixer != 0`) before adding, and logs add-failures. Sharpens the multi-send boundary: with two buses *evaluated simultaneously* collecting the same source they now visibly steal it from each other per frame (routing-log spam) — correct behaviour needs the split-stream mechanism (§Realisation); until then a source is audible through one bus at a time. Known limitation: `[AudioLevel]` can't meter a graph-routed `[AudioClip]` (its channel is added un-buffered) — but the bus `Level` output covers metering a bus that contains clips.

- `[AudioReverb]` + bus `Level` user-tested ✅ (committed). Note: DX8 reverb decay is hard-capped at 3 s (`fReverbTime` ≤ 3000 ms) — longer tails need the BASS_FX backend swap.
- `[AudioEcho]` (Mix/Delay/Feedback/PingPong → DX8 echo; delay ≤ 2 s is a DX8 cap) and `[AudioCompressor]` (linear Threshold like the meter ops → dB, Ratio, Attack/Release secs, MakeupGainDb) landed 2026-07-31 — same insert mechanism, needs live test.

**Phase B started (2026-07-31) — decision: FX-op family is sufficient for now; more effects later.**

**B1 — Display/Style + main-soundtrack union (landed, needs live test):**
- `AudioClipDisplay { Clip, BackgroundImage }` + `AudioClipStyle { Spectrum, Waveform, VolumeLevel }` enums in `Core/Audio/TimelineAudioClip.cs`, with transient `Display`/`Style` fields on the clip (op-synced, not serialized). `Style` currently defaults to `Spectrum` since that's the only renderer; revisit the default when the waveform renderer lands (open question 4).
- `[AudioClip]` gained `Display`/`Style` inputs; **`Display = BackgroundImage` doubles as the main-soundtrack designation** (`_clip.IsMainSoundtrack` sync). This *is* the op-side flag the union needs — no separate settings-Guid yet (the §Retire-IsMainSoundtrack (2) Guid-in-settings + context-menu design remains the fuller follow-up).
- `CompositionSettings.TryGetMainSoundtrack` unions the settings list with op-provided clips (scans the settings owner's children for `IAudioClipProvider` whose clip flags main). All four call sites (timeline background, export, render timing, settings window) get it for free.
- Known gap: the union only reaches ops in a composition whose settings are `Enabled` (the `FindCompositionSettings` walk-up). A background-image clip in a project that never enabled project settings won't show — fold into the B2/§Retire design.

**B3 — Style renderers (landed 2026-07-31, needs live test; editor restart required — Editor-assembly change):**
- `AudioImageGenerator.TryGenerateClipImage(clip, instance, style, out path)` dispatches per style: `Spectrum` = the existing FFT path (cache suffix `.waveform.png` kept so old caches stay valid); `Waveform` = symmetric peak columns with a softer RMS band inside (`.amplitude.png`); `VolumeLevel` = bottom-filled, lightly smoothed RMS envelope (`.level.png`). Amplitude scan decodes sequentially in ~10 ms columns (offline stream is `BassFlags.Float`, plain byte counts). Both new styles render white-on-transparent so the renderers' theme tint applies.
- `AudioImageFactory` caches per `(assetPath, style)`; `TimeLineImage` (background) and `AudioClipBodyRenderer` (clip body) pass the clip's synced `Style` through — style switches load a different cached image.

**Export fix — graph audio in video render (landed 2026-07-31, needs re-test):** graph audio was silent in exported videos (one blip, then silence; bus `Level` frozen). Root cause: export only evaluates the exported op-chain, so a bus kept alive by a pinned view stops being evaluated → misses its `AudioBusRegistry` heartbeat → its submix is pause-swept after the 2-frame slack. Fix: revived the half-finished `AudioExportSourceRegistry` mechanism — `IAudioExportSource` is now `{ bool IsActiveForExport }` (unused `RenderAudio` dropped), `[AudioBus]` registers itself on update, and `AudioRendering.PrepareRecording` snapshots the buses organically alive at export start (≤10 frames); those get force-evaluated every exported frame **before** the mixdown pull (`RenderProcess` reordered — evaluation used to run after, off-by-one), so routing, animated params, trigger edges and FX all drive the exported audio. Buses that were already stale-silent stay silent, matching live. Note: export audio still bypasses per-bus master `Volume` only insofar as the mix reads the whole OperatorMixer — bus volume/gains apply inside the submix chain, so they're included.

**Metering constraint (found via log probes, 2026-07-31): the plain `BassMix.ChannelGetLevel(handle)` is unusable for per-frame metering.** It measures only data taken *since the last call*, and the device pulls the mixer chain in coarse chunks — so a per-frame reader mostly lands between pulls and reads 0, with occasional correct spikes (seen as the bus `Level` erratically flat/zero while its FX submixes carried signal). It's also consuming, so two readers on one channel additionally fight over the window. Rule: **meter with the buffer-inspecting Ex overload** — `BassMix.ChannelGetLevel(handle, float[2], 0.05f, flags)` (returns −1 on failure; needs `MixerChanBuffer`) — it's non-consuming and stable per frame. Applied in `[AudioBus]` (still measured once per frame in `Update`, `Level` reports the cached value), `[AudioLevel]`, and `[AudioToneGenerator].GetLevel`.

**Export × graph-routed clips (landed 2026-07-31, needs re-test):** `AudioRendering.PrepareRecording` used to steal *every* soundtrack clip stream into the export mixer — including graph-routed ones — which (a) stripped the graph's gain/duck/FX from the exported clip audio and (b) stranded the channel in the SoundtrackMixer after `EndRecording` (audible, but gone from the bus and its meter, since the bus's bookkeeping still said "routed"). Fix: all three export loops (`PrepareRecording`/`EndRecording`/`MixSoundtracksFromExportMixer`) skip clips with `IsRoutedToGraph` — a graph-routed clip stays in its bus submix and reaches the export through the OperatorMixer read. Plus **bus self-healing**: the reconciler now verifies via `BassMix.ChannelGetMixer` that each supposedly-routed channel is actually in its target mixer, re-adding when another subsystem moved it — so external stealing can no longer permanently desync the bus. Follow-up (same day): the exported routed clip was still silent — during export the engine skips `UpdateSoundtrackTime` for all clips (export-mixer positioning was assumed) *and* the render transport's `PlaybackSpeed == 0` had pause-flagged the channel with nothing left to unpause it. Fixed: `ProcessSoundtrackClips`' export branch now runs `UpdateSoundtrackTime` for `IsRoutedToGraph` clips (their `TargetTime` arrives via the forced bus evaluation), and `UpdateSoundtrackTime`'s speed-0 pause exempts recording (same rule as the bus transport gating).

**B2 — settings-list migration (landed 2026-08-01, reworked after review, needs live test):** `Editor/UiModel/LegacyAudioClipMigration.cs`, running **once in the startup-migration phase** (`ProjectSetup.Startup`, beside `VariationsMigration`, after all packages are registered so the `[AudioClip]` symbol resolves). The first save-path integration was rejected in review — hooking `SaveAll`/`SaveModifiedSymbols` ran the mutation during recompilation flows and widened auto-save write scope. Now the migration is in-memory + `FlagAsModified`; the *regular* save machinery persists it whenever the user next saves (matching the clean-on-load / persist-on-save idiom properly). Only editable projects are touched (read-only packages can't persist the change). Asset resolution at startup creates the parentless instance on demand for affected symbols. Per legacy clip: creates a visible `[AudioClip]` child (`AddSymbolChildCommand` + `ChangeInputValueCommand`, executed but **not** pushed to undo — a save-time migration isn't a user edit), copies Path/Volume/Mute/TimeRange/LayerIndex/SourceOffset, AutoPlay on; former main gets `Display = BackgroundImage` + `Style = Waveform`. Details: the legacy "no explicit end" sentinel (`End <= Start`) is resolved to real content length (BASS offline probe when `LengthInSeconds` is 0; probe failure defers that clip to the next save instead of losing it); `SourceRange` anchored at `SourceOffsetSecs` in bars, 1:1; migrated entries removed from the list; **`settings.Enabled` is forced true** (else the emptied settings block would be dropped from the file, losing the BPM and hiding the op-flagged soundtrack from the settings walk-up). Empty-path entries are dropped. Forward-compat note: a migrated project opened in an older TiXL has no settings-list soundtrack → silent there (accepted).

**Player support for op-audio (landed 2026-08-01, needs export test):** the standalone Player only played settings-list clips — a migrated project would have been *silent* as an exported executable (`RegisterAutoPlayClips` was editor-only). Fixed: the Player render loop now calls `AudioClipCollector.RegisterAutoPlayClips` + `AudioGraphCollector.CollectLooseSources` per frame (graph ops wired into the render chain already worked — they evaluate, and `CompleteFrame` runs in the Player), and Player startup falls back to the `TryGetMainSoundtrack` op-union for `_soundtrackHandle` (stream preload + end-of-timeline check). Exporter side needed nothing: `TryFindSoundtrack` already goes through the union, and the op's FilePath-usage `Path` input gets its asset collected.

**Small-items batch (landed 2026-08-02, needs live test):** ✅ "Add soundtrack to composition" creates a visible `[AudioClip]` op (AutoPlay + BackgroundImage, undoable macro; picking the first file also sizes the clip's TimeRange from the probed duration — no more zero-width block). ✅ Timeline context menu **"Set as main soundtrack" / "Unset main soundtrack"** on a single selected audio clip — sets `Display = BackgroundImage` and clears every other audio clip's designation in one undoable macro (single-main invariant; ops identified by the Display input id). ✅ `[AudioLevel]` `IStatusProvider` warning when a side-branch tap collects an unmeterable (engine-owned) clip leaf. ✅ Player end-of-timeline honors an explicitly trimmed `TimeRange.End` over file length. ✅ Test sets: loop/trim-extent/set-main steps in `timeline-audio-clips.md`, compressor step in `audio-graph-routing.md`, new `timeline-editing.md` (follow/jumps/ripple/split).

**Playback-settings redesign (landed 2026-08-02, needs live test):** BPM / timeline-display / audio-decay always visible (no soundtrack required); no-soundtrack state shows a "multiple soundtracks since 4.3" hint + *Add a Soundtrack* (creates the op at 0.0 with BackgroundImage+Waveform and selects/centers it in the graph); with an op soundtrack a *Select Operator* button jumps to it; the separate **"Audio" category is merged into "Playback & Audio"** (mix levels under a "Mix" subheader — tab still reads "Playback"); the `ui:ProjectSettings` help topic rewritten with the new audio workflow (drag-to-timeline, clips, AutoPlay/AutoCollect, AudioBus, effects) — surfaces through the window's existing `HelpUiID`.

**Project-settings restructure per final mockups (landed 2026-08-03, needs live test):** supersedes the 2026-08-02 "Playback & Audio" merge. Sidebar is now **Timing / Audio / Proxies / Recording / Export**. *Timing:* "Project Setup: Animation | Live Interactive" segmented control — pure UI mapping onto the serialized `AudioSources` enum (Animation=ProjectSoundTrack, Live=ExternalDevice), per-mode hint; Animation shows BPM + Timeline Display; Live shows Sync Mode (Timeline → "BPM-Rate" + Timeline Display; Tapping → "Default BPM-Rate" behind the beat-lock gate) + Beat Sync Offset. *Audio ("Project Audio"):* "Playback" (Resync Threshold + single **Main Volume** writing both `SoundtrackVolume` and `OperatorVolume`; mute checkboxes dropped from UI, fields still serialized), "Audio Analysis and Reactivity" (Input/Default Device, Gain, Decay, Level meter — moved out of the Live playback panel so reactivity is configurable in Animation mode too), "Project Soundtrack" (hint + **Create Soundtrack** / **Select and focus Main Soundtrack**). The settings-side soundtrack editing UI (path picker, Reload/Remove, Offset) is **gone — the op owns all of it**; deleted `InitSoundtrackClipRangeIfNeeded`/`TrySetSoundtrackOpInput`/`UpdateBpmFromSoundtrackConfig`. Enabler: a pickerless Create needs the sentinel clip to still load its stream — `AudioClipCollector.RegisterIfActive` treats `End <= Start` as "no explicit end" (plays from Start), and `[AudioClip].SyncClip` sizes the sentinel TimeRange/SourceRange to the file duration once `LengthInSeconds > 0` (replaces the settings window's offline probe). Open from mockups: the Animation variant showed a **"UI Scale"** field — unclear if that's a project-level scale; not implemented, ask.

**Background-mode rework (landed 2026-08-03, needs live test):** user decision after first live test — a `Display = BackgroundImage` clip showing *both* the full-height background *and* its clip block was redundant. New model: **background mode = locked as soundtrack**. (a) `[AudioClip].SyncClip` auto-extends the TimeClip to the full source duration (Start preserved as offset; SourceRange reset to full file) once `LengthInSeconds` is known; (b) `ClipSelection.UpdateForComposition` excludes the *effective* main (same `TryFindingSoundtrack` walk as the background renderer) from `CompositionTimeClips`, so drawing, selection, fence, split and `.`/`,` jumps all ignore it — a flagged-but-losing duplicate stays visible as a soft conflict hint; (c) editing workflow is mode-switch: set `Display` back to `Clip` to reposition, then re-flag (note: a pre-existing trim is overwritten by the auto-extend, not restored). Related fixes same day: `TimeLineImage` falls back to `AudioClipDurationCache` when `LengthInSeconds` is 0 (op clips only got a length after playing once — background never showed), ignores clip-trim clamping in background mode, and the settings window's soundtrack button uses `TryFindingSoundtrack` + `OpenAndFocusInstance` (old selection+fit only worked when the op's composition was already open). Settings soundtrack hint renders as `MarkdownView` (op-linked `[AudioClip]`, no lightbulb/indent). Still queued: `IStatusProvider` warning on `[AudioClip]` when multiple siblings are flagged main.

**Device-change handle invalidation (landed 2026-08-03, needs live test):** switching the audio input device (`AudioEngine.OnAudioDeviceChanged`) tears down BASS entirely (`AudioMixerManager.Shutdown` → `Bass.Free`) — the engine recreates its own streams, but `[AudioBus]` cached dead submix/FX handles and spammed "failed to route channel: Handle" every frame. Fix: `AudioMixerManager.ResetGeneration` (bumped in `Shutdown`); the bus compares per update and on mismatch drops `_submix` (+ registry unregister), FX groups (with insert `Remove` so the FX ops clear their per-submix dicts), routed targets and pending frees — everything rebuilds lazily on the new device. Rule for future ops: **any op caching BASS handles across frames must check `ResetGeneration`.**

**Settings-decoupling follow-up (deferred 2026-08-02):** `CompositionSettings.Enabled` doubles as the *write-permission* for per-symbol editor state in `.t3ui` (`TimelineState` incl. loop range, `RenderSettings`) — states set on a never-enabled root are silently dropped on save. This rarely bit while audio forced `Enabled` early; now that audio works without settings (op-union, auto-collect), it will. `OutputWindowStates` (pinning) was exempted 2026-08-02 (root-only state, no pollution risk). For the rest, decide deliberately: write-when-non-default, or editing loop/render settings implicitly marks the symbol configured. Don't just drop the gate — it protects sub-op `.t3ui`s from per-visit state pollution.

**Tap helpers on `AudioGraphNode` (landed 2026-08-09, unverified):** `DeclareAnalysisTap()`, `RealisedSubmixes`, `TryGetAnalysisChannel()` and `HasExternallyManagedLeaf()` moved onto the node — the effect-less-insert boilerplate and the submix-first/leaf-fallback rule were duplicated (and already diverging) between `[AudioLevel]` and the new `[AudioReaction]` tap. `[AudioLevel]` now uses them; its side-branch loop stays op-local because it needs per-leaf gains for a max level, which an FFT tap doesn't. **`[AudioLevel]` was working before this refactor and has not been re-tested since** — re-run `.tests-manual/audio-ducking-metering.md`.

**G4c — `[PlayAudioSample]` in the graph (landed 2026-08-09, needs live test):** the G4b pattern applied to the *operator-stream* path rather than the soundtrack one, which needed three Core additions rather than a transcription: `AudioEngine.TryGetOperatorChannel` (the stream handle is `internal`, so an operator couldn't reach it); `OperatorAudioStreamBase.GraphOwnsVolume`, which suppresses all four of the engine's `ChannelAttribute.Volume` writes (`Play`, `SetVolume`, `SetStale`, `RestartAfterExport`) because the bus writes that *same* attribute for node gain and the two would fight every frame; and `ReclaimMixerMembership`, since `TryLoadStreamCore` routes the stream into the operator mixer itself and un-routing otherwise leaves the channel in no mixer at all.

Ownership splits: **gain → the node**, envelope included (`node.Gain = envelopeModulatedVolume`), so group volume, ducking and FX apply; **pan / speed / seek / triggers → the engine**, since they are different channel attributes and the graph has no concept of panning. Unrouted behaviour is unchanged. Routed detection mirrors `[AudioClip]`: wired **or** stamped by an auto-collector within 2 frames.

**Reference-as-heartbeat + output order (fixed 2026-08-09, found in live test):** first cut only drove playback from `Result`, so wiring *just* `AudioReference` into a bus produced silence — the op was never evaluated. `UpdateAudioReference` now drives `UpdatePlayback` as well (frame-keyed, since both outputs can reach it in one frame and a double run would consume the same `PlayAudio` edge twice and desync the envelope). `AudioReference` is also declared first, so it is the primary output — routing the sound is what the operator is for.

**Why `Result` stays.** It is the only thing that evaluates an *unrouted* sampler. Removing it would make an `[AudioBus]` mandatory, and since a bus must itself be evaluated (pinned or in the render chain), the simple case would go from one connection to two plus an extra op. The alternative — implicit collection via `IAudioSource` — is ruled out below for the same reason it was for `[AudioToneGenerator]`.

**Deliberately not an `IAudioSource`.** Implementing it would have the implicit default bus route the sampler while its own operator-stream path already does, and the two would steal the channel from each other; it would also make `AutoCollectClips` sweep it into a group. Same call as `[AudioToneGenerator]`: implicit audibility is for timeline-presence sources, not procedural ones. A sampler is wired explicitly or not at all.

**Bypass actually bypasses (fixed 2026-08-09, found in live test):** `AudioGraphNode` was added to `Symbol.Child._bypassableTypes` during the G-step hardening, so the editor offered bypass and drew the strike-through — but `Instance.SetBypassFor`'s type switch had no arm for it, so it fell through, swapped no update action, and the effect kept processing while the UI showed it disabled. `Symbol.Child.SetBypassed` records `_isBypassed` regardless of the result, so nothing surfaced the discrepancy. Added the arm; a bypassed FX op now emits its upstream node, the bus stops collecting the FX node, and the group retires with the usual tail fade. **Known limit, consistent with the other multi-input types (`Command` etc.): a bypassed combinator passes through its *first* input only.** Second defect found immediately after, in the same live test: **un**-bypassing left the op neutered. `ByPassUpdate` overwrites the output's `Value` with the upstream node, and `RestoreUpdateAction` restores only the *action*, not the value — so ops that assigned `Result.Value = _node` once in their constructor never published their own node again. Every node-emitting combinator/tap now re-publishes it on each update. Worth remembering as a general rule: **a slot whose value is a constant object set at construction is not bypass- or disable-safe; publish it from the update action.** Note `ShaderGraphNode` has the identical latent bug — same list, same missing arm — left untouched as it belongs to the field/shader work.

**Auto-collect generalised to any graph source (landed 2026-08-09, needs live test):** `LooseAudioClipScanner` matched `IAudioClipProvider` only, so `AutoCollectClips` on `[AudioBus]`/`[CombineAudio]` picked up timeline `[AudioClip]`s but never a `[VideoClip]`'s or `[PlayVideo]`'s sound — those had to be wired by hand. It now also matches `IAudioSource`, so any op exposing a graph source joins the group on the same terms (the tone generator implements neither, per its deliberate graph-only decision, so it is still never swept up). Enabler: `AudioGraphCollector` now skips sources whose node was stamped by an explicit collector within 2 frames (`LastCollectedFrame`, which `[AudioBus]` already sets on every leaf) — without that guard the implicit default bus and the auto-collecting op would add the same channel to different mixers every frame and steal it back and forth.

**Export starts clean (landed 2026-08-09, needs live test):** a reverb or echo still ringing from live playback bled into the first exported frames — audible as a fade-out at the head of every render, and a source of run-to-run difference. `IAudioExportSource` gained `ResetForExport()`, called from `AudioRendering.PrepareRecording` on the snapshotted sources; `[AudioBus]` implements it with `Bass.FXReset` on each FX submix and on its own submix (passing the *channel* resets every effect on it, so the bus needs no knowledge of what the FX ops applied). The video feeder does the equivalent for its own queue — see the video-audio plan.

**Device-change fix (landed 2026-08-09, unverified):** `AudioGraphCollector` never invalidated its static `_defaultBus` on `AudioMixerManager.ResetGeneration`, so after an audio-device change the dead handle was reused forever and *every* loose graph source — `[AudioClip]`, tone generator, video — stayed silent until restart. Explicit `[AudioBus]` routing was unaffected (it already checked the generation), which is why this went unnoticed.

**Next:** live-test migration on a real legacy project (play + background waveform + FFT before/after save, reopen), **plus an executable export of a migrated project** (sound, end-of-timeline/loop). Then Phase D (reference lines) / `Direct` spatial realization / `IsMainSoundtrack` consumer retirement per plan.

## Graduation steps (spike → real ops)

Incremental — each step compiles and is testable on the last. Fork choices favour the simplest always-working path; the richer variant is the named follow-on.

- **G1 — Contract foundation (Core, additive).** ✅ Add `RoutingKind { Mixable, Direct }` to `AudioGraphNode` (the wire value) — the one field expensive to retrofit once the wire ships (interface-stability audit). Default `Mixable`; `Direct` handled in G5. Spikes keep working. *Test: no regression.*
- **G2 — Real combinator `[AudioGroup]`.** Replaces `_AudioGroupSpike`: folds `MultiInputSlot<AudioReference>` with a group `Volume`. **Fork A — group gain:** fold gain *down into* collected sources (flat root submix, no group FX) now; group-as-its-own-submix (reverb-on-a-group) is the follow-on in G5. *Test: two tones through an `[AudioGroup]` with group volume → volume scales both.*
- **G3 — Real root `[AudioClipPlayer]`** (hosts `GenerateAudioProcessingBlock`). Replaces `_AudioRootSpike`: the reconciler (per-frame diff; `StructureHash` gating deferred as a perf step), master volume, and **AutoCollect** (scan composition children for sources → tier-1, like `AudioClipCollector`). Honours `RoutingKind.Direct` (skip the submix). *Test: tones play wired and unwired (AutoCollect).*
- **G4 — Real audio source.** **Fork B — first real audio:** a minimal file-source op (`.wav` → BASS channel → node) first, to prove real files (path resolution + real streams) through the graph; then integrate the canonical `[AudioClip]` (expose its `SoundtrackClipStream` channel; keep the existing `AudioEngine` playback path working during transition). *Test: a wav plays, routed/grouped, following the graph.*
  - **G4a** ✅ `_AudioFileSpike` — plays a `.wav`/`.mp3`/`.ogg` as a looping decode stream, emits its channel on a node; routable or loose-auto-played. Real files proven through the graph.
  - **G4b** ✅ Canonical `[AudioClip]` emits an `AudioReference` (new output). **Approach: reuse the soundtrack path, don't reimplement clip timing.** The `AudioEngine` still creates the `SoundtrackClipStream` and position-syncs it every frame (position-sync is a *per-channel* `BassMix` op — works in whichever mixer holds the channel). When the clip's `AudioReference` is wired into a bus (`AudioClip.IsAudioReferenceWired()` → `TimelineAudioClip.IsRoutedToGraph`), the graph takes over **membership + level only**: the `[AudioBus]` moves the channel out of the SoundtrackMixer into its submix (un-buffered — `MixerChanBuffer` latency would break seek/resync; flagged via `AudioGraphNode.ExternallyManagedChannel`) and applies gain, while `SoundtrackClipStream.UpdateSoundtrackTime` skips its own volume write. Unwired → unchanged soundtrack playback (**full back-compat**). New Core surface: `AudioEngine.TryGetSoundtrackChannel`, `TimelineAudioClip.IsRoutedToGraph`, `AudioGraphNode.ExternallyManagedChannel`; a graph-routed clip is created *not* added to the SoundtrackMixer (avoids a 1-frame full-volume blip); on unroute the engine reclaims the channel (`_wasRoutedToGraph` transition). **Caveats to revisit:** (1) ✅ *fixed* — bus-as-heartbeat: the clip's `AudioReference` update calls `AudioClipCollector.RegisterIfActive`, so wiring a bus is enough to play (no AutoPlay/player); the bus itself must still be evaluated (pinned or wired into the render chain); (2) a graph-routed clip's level bypasses global SoundtrackMute/AppMute/AppVolume (only per-clip Volume×Mute and bus Volume apply) — "mute all" won't silence it yet; (3) hard-deleting the bus (vs unwiring) may leave the clip briefly silent until the freed submix is GC'd; (4) routing the *main* soundtrack clip (FFT/waveform source) through a bus is untested. *Test: an `[AudioClip]` with AutoPlay on, wired into an `[AudioBus]` → the clip plays through the bus at bus volume (moves off the soundtrack path); unwire → it returns to normal soundtrack playback.*
- **G5 — Tier-3 richness.** Group-as-submix + FX inserts (reverb), `Direct`/spatial realization, multi-send via split streams, `[Duck]`/`[AudioLevel]` metered control — each its own increment.
- **G6 — Retire spikes + wire to output.** Delete `_Audio*Spike`; connect the real root into the timeline/render output; then migration + Display/Style + reference-lines per their sections.

## Op taxonomy & naming (cleanup pass — after the graph ops land)

Naming rule: **verb+`Audio` for operations** (transform/combine references), **`Audio`+noun for things** (a clip, a bus), **`Play…` for trigger/event sample players**. Renames are GUID-safe (existing instances survive a rename); only *removals* need migration.

- **Sources** (emit `AudioReference`):
  - `AudioClip` — keep.
  - `AudioPlayer` → **`PlayAudioSample`** ✅ (haas' sampler; rename and `AudioReference` output both done — see the G4c note below).
  - `SpatialAudioPlayer` → **`PlaySpatialAudioSample`** ✅ (rename done; `Direct` routing kind — HW-3D, bypasses the bus — still pending).
  - `AudioToneGenerator` ✅ — fixed + integrated as a graph source (took over the `_AudioSourceSpike` role).
- **Combinators** (fold references):
  - **`CombineAudio`** ✅ (was `AudioGroup` → `GroupAudio`) — combine sources + volume fold.
  - **`BlendAudio`** (new) — crossfade N inputs by one float index (like `BlendScenes`); weights the two adjacent inputs internally. A weight-*list* **`MixAudio`** is the later general case (reuses the same per-child weighting).
  - later: `MixAudio`, `AudioReverb` / effects, `Duck`.
- **Output:** tier-1 is *implicit* (no op → no "why is it silent / where's the player?" confusion). The tier-3 explicit bus = **`AudioBus`** (replaces the `AudioClipPlayer` role — it collects *all* sources, not just clips, and isn't required to play them). Optional; multiple allowed; `AutoCollectSources` **exclusive** (one bus auto-collects, so no double-collection). Verify the implicit path covers nested-comp + export before finalising (the historical reason `AudioClipPlayer` existed).
- **Analysis:** `AudioReaction` — **keep the name** (×30 uses); extend with an optional `AudioReference` input to analyse a specific source/bus instead of only the global mix.
- **Remove:** `PlayAudioClip` ✅ (hard-deleted; asset-drop mapping moved to `PlayAudioSample`).

## Architectural decisions (locked in)

- **All clips are ops.** The `[AudioClip]` operator is canonical (reverses `Plan_TimelineAudioClips.md`'s "delete the op" step). The legacy `CompositionSettings.Playback.AudioClips` settings-list drops to **migration-source-only** and is deleted once projects are migrated.

- **No shadow / hidden ops.** (Considered and rejected — Tooll2 had hidden instances and they brought debugging, selection, and consistency drawbacks.) Migration creates an ordinary **visible** `[AudioClip]` op; a typical project has a single main soundtrack, so that's one node, not clutter. Where a project genuinely has many audio ops, density is managed by the **existing, user-controlled collapsed sections** — deliberate and reversible — never by automatic hiding.

- **New connection type `AudioReference` is a *structural node*, not a value.** Modelled on `ShaderGraphNode` (`Core/DataTypes/ShaderGraphNode.cs`), not on `RenderTargetReference`. It flows on `Slot<AudioReference>` and carries: either a reference to a **source** (anything exposing a routable BASS channel — a clip stream, operator/proc stream, or an op-internal submix) *or* references to upstream input nodes, plus a **routing kind** (`Mixable` / `Direct`), animatable params, and a `StructureHash` + `ChangedFlags`. **No PCM ever flows through graph evaluation.** BASS is the executor ("the GPU of audio"); the wire carries structure only. A connection type in TiXL is just a C# value type registered for a wire colour via `TypeUiRegistry.SetProperties(...)` (`Editor/UiModel/UiRegistration.cs`).

- **Collection is root-driven and off the per-frame `Update()` path.** A root (`GenerateAudioProcessingBlock`, hosted by `[AudioClipPlayer]` / the audio output) initiates a bespoke traversal — recursive node `Update()` then a collect pass — exactly like `GenerateShaderGraphCode`. The reference output must be collectable **independent of the playhead** (unlike the current `TimeClipSlot<Command>` output): audibility is computed by the root from each clip's `TimeRange`, not from whether the op's `Update` ran. **The root is responsible for updating the animatable inputs of every node it collects** — mirror `ShaderGraphNode`'s `foreach (paramInput) paramInput.Update(context)`; skip this and automation silently freezes. **Validated (2a spike — `_AudioSourceSpike`/`_AudioGroupSpike`/`_AudioRootSpike`):** a root collecting through group combinators logs the correct recursive source set (`A, B, A` for a source reached via two paths), updating live as the graph is rewired — R1 confirmed, off the per-frame path. Hardening TODO before graduation: register `AudioGraphNode` in `Symbol.Child._bypassableTypes` and `TypeUiRegistry` (color), matching `ShaderGraphNode` — a transient NRE appeared mid-wiring while these were absent.

- **Two collection paths, both with existing infra:**
  - *Implicit (tiers 1–2):* `AudioClipCollector.RegisterAutoPlayClips` already scans `composition.Children` for `IAudioClipProvider` and runs in the Player/export loop too. Works regardless of graph wiring (it scans the instance graph, not the UI). **No connections required.**
  - *Explicit (tier 3):* the `AudioReference` graph, collected by the root.
  - *Precedence:* an explicit root owns any clip it reaches; the implicit collector claims only clips no explicit root took. A clip reached by two roots is an intentional multi-send — the root realises it via split streams internally (§Realisation), so it's user-invisible.

- **`[AudioClip]` display is an explicit setting, not magic.** Two per-clip params replace the overloaded `IsMainSoundtrack` background behaviour:
  - **Display:** `Clip` (a normal timeline block) or `Background image` (full-timeline-width behind the tracks — what the main soundtrack renders as today).
  - **Style:** `Waveform` (amplitude — the renderer TiXL lacks today; only an FFT spectrogram exists), `Volume level` (filled envelope, Audacity / FCP-style), `Precise` (full-resolution waveform on zoom — long-term).
  This extracts the *background-render* concern out of `IsMainSoundtrack` (a documented tech-debt bundle of background-render + FFT-routing + export). Display only solves the render concern; FFT-source and export-inclusion still need their own explicit homes (see open questions).

- **Implicit-relationship lines + "realize" — a general graph feature, not audio-specific.** Auto-collected clip→root relationships render as **faint dashed reference lines**; one action materialises a line into an explicit wire (tier 1 → tier 3), with an inverse that drops the wire back to implicit. The handoff must be glitch-free (no double-play or dropped frame). The same affordance is wanted for **Set/Get Variable** ops and the requested VVVV/Houdini-style **Send/Receive** ops (named wireless links). Build the line rendering + "realize" as a shared graph feature; audio is its first consumer. Note the two underlying flavours it must cover: a *latent real connection* (audio auto-collect — realises into a typed wire) vs a *name binding* (variables / send-receive — may stay wireless, but could also realise into a direct wire to its source).

- **Ducking / sidechain = a metered float, not a structural edge.** A Duck op reads the *last frame's* level of a bus (the engine already meters via `Bass.ChannelGetLevel`) and drives a gain — the ~1-frame lag is inherent to metering and inaudible for ducking. This keeps the reference graph a pure tree, and even makes mutual sidechain degrade to a stable discrete feedback loop instead of a hard cycle. Only *sample-accurate* keying (a look-ahead limiter keyed off another bus) stays structural, and is deferred. Cleanest form: an `AudioLevel(ref) → float` tap op + plain float math into a Volume, so no special "signal vs key" reference variant is needed.

- **Realization is a *diff against live state*, not a rebuild.** This is where the audio version is genuinely harder than the shader graph (which is stateless pure-compute). `ChangedFlags` drive an incremental reconciliation of the BASS mixer: `Structural` → add/remove/reroute streams, insert/remove FX (fading FX tails so a removed reverb doesn't truncate); `Parameters` → set scalar volumes/pans every frame unconditionally (cheap — no compile to protect, so no per-param caching needed). This reconciliation is a *superset* of what `AudioEngine` already does (heartbeat, stale-eviction, resync-threshold), so it extends existing code rather than starting fresh.

- **Back-compat:** no forced load-time mutation. Migrate on save (the file already follows "clean in memory on load, persist on next save" — see `CompositionSettings.GetClips` dead-clip pruning). `CompositionSettings.TryGetMainSoundtrack` generalises to a **union** of the settings-list and any op flagged main, so the background-waveform / FFT / export lookups keep working for old and new projects. Existing playback and audio images are untouched.

## Data model

`AudioGraphNode` (new, `Core/Audio/` — the value carried by `AudioReference`), shaped after `ShaderGraphNode`:

```csharp
sealed class AudioGraphNode
{
    // Leaf: any source exposing a routable BASS channel — a clip stream, an
    // operator/proc stream, or an op-internal submix (a future polyphonic sampler
    // sums its own voices and exposes one channel here). Null for pure combinators.
    IAudioSourceChannel? Source;

    // Direct sources (spatial HW-3D) route to the output/3D engine, not a submix,
    // and can't take a bus FX insert. Mixable is the default.
    RoutingKind Routing;                  // Mixable | Direct

    // Combinator: upstream nodes folded into this one (from the op's input slots).
    List<AudioGraphNode> InputNodes;      // rebuilt each traversal; drives StructureHash

    // Animatable params collected by the root each frame (volume, pan, sends, FX params).
    // Analogous to ShaderGraphNode's [GraphParam] inputs.
    // ...

    int StructureHash;                    // detects rewiring without DirtyFlagTrigger
    ChangedFlags CollectedChanges;        // Structural | Parameters (no "Code" — no compile)
}
```

Reused unchanged from `Plan_TimelineAudioClips.md`: `TimelineAudioClip` (the clip payload the handle wraps), `AudioClipResourceHandle`, native-rate playback, `TimeRange`/`SourceOffsetSecs`/`SourceDurationSecs`. `Display`/`Style` are ordinary inputs on `[AudioClip]`.

Placement math is already converging on the shared `TimeRangeMapping` struct (`Core/Animation/TimeRangeMapping.cs`), whose doc already names audio/video/data/image-sequence as intended consumers.

## Operators

- **Package placement (found during the spike):** audio-graph ops call `BassMix` directly, so they must live in a BASS-referencing package (`Lib`, like `AudioToneGenerator`). User/Playground projects set `DisableTransitiveProjectReferences` and reference only Core/Logging/SharpDX — they *cannot* call BASS. (Alternative: Core exposes sub-bus routing helpers so any package can drive audio.)
- **`AudioReference`/`AudioGraphNode` must be a *Core* type (found during 2a):** connection/input types are registered centrally in `Core` `SymbolPackage.RegisterTypes` (`InputValueCreators`, mirroring `ShaderGraphNode`). A Lib-defined type can't be registered there, so a `MultiInputSlot<AudioGraphNode>` input fails to build its input definition ("can't create Input Definition"). Outputs are unaffected — only inputs need the registration. So the wire type lives in `Core/DataTypes` (beside `ShaderGraphNode`); the ops live in `Lib`.
- **Sources (leaves):** `[AudioClip]` (timeline clip; `Display` + `Style` params). The trigger-driven operator-audio ops — `[AudioPlayer]`, `[AudioToneGenerator]` (a procedural synth), `[PlayAudioClip]` — become non-timeline source nodes that *also* emit `AudioReference`, so a triggered synth voice and a timeline clip route / mix / FX through identical machinery. **Monophonic today** (1 op = 1 voice = 1 reference); trigger / ADSR / pitch stay on the op, below the reference (the reference is its output bus). `[SpatialAudioPlayer]` is a routing exception — a `Direct`-kind source (§Data model).
- **Combinators:** `[AudioGroup]` / `[AudioMix]` (fold `MultiInputSlot<AudioReference>`, adjust gain/pan), `[AudioGain]`, `[AudioReverb]` / `[AudioEffect]` (declare an FX insert on the collected bus).
- **Root:** `[AudioClipPlayer]` hosting `GenerateAudioProcessingBlock` (≈ `GenerateShaderGraphCode` for audio). Traverses, collects, reconciles BASS.
- **Metered control:** `[AudioLevel]` (bus → last-frame float), `[Duck]` (level → gain envelope). Ordinary float dataflow, on the normal `Update()` path.

## Realisation → BASS (the load-bearing new work)

- **Validated (spike `_AudioBusSpike`, Lib/io/audio):** dynamically creating decode submixes nested under the operator mixer, routing a source through one, per-bus gain via `ChannelSetAttribute`, and **glitch-free live reroute** (`MixerRemoveChannel`→`MixerAddChannel` on a *playing* source) all confirmed working by ear. The core reconciliation mechanism — the one part with no codebase precedent — is proven. Remaining work is doing it *at scale*: many sources/buses, add/remove per frame, FX inserts with tail fades. (The reroute was click-free in the simple case; still ramp gains on structural changes as a safety margin under heavier rerouting.)
- **What the reconciler fundamentally does:** every source already produces a BASS channel (clip stream / operator stream / proc stream / op-internal submix); today each op *hard-codes* which mixer its channel joins (`OperatorMixer` / `SoundtrackMixer` / direct-3D). Realisation makes that a routing decision the root owns — reconciliation is the set of `MixerAddChannel` + FX-insert + gain calls that plug each collected source's channel into the resolved (sub)mixer. Sources keep producing channels exactly as today; the root owns the mixer topology.
- **Direct sources** (`RoutingKind.Direct`, e.g. spatial HW-3D) route straight to the output / 3D engine, bypassing submix insertion. A group/FX node that collects one respects its gain but can't bus-insert it (a validation hint, not a silent stereo-collapse).
- `[AudioGroup]` → a `BassMix` submix; `[AudioReverb]` on it → `ChannelSetFX` on that submix; the root plugs the submix into master.
- Params (volume/pan/FX mix) → `ChannelSetAttribute`, set every frame unconditionally.
- Topology mutations (create/destroy/reroute streams, insert/remove FX) → gated on `StructureHash` change only.
- **Multi-send** (decided — allowed, handled internally): a source feeding two groups → BASS split streams (`BASS_Split_StreamCreate`), since a decode source sits in only one mixer. The root's reconciler owns the splitters entirely — the user just wires a source into two buses and it sums; the split-stream mechanism is never exposed.
- FX removal fades the tail rather than truncating; stream add/remove reuses the existing stale/resync path.

## Migration (settings-list → visible ops)

- For each `TimelineAudioClip` in `Playback.AudioClips`, create an ordinary **visible** `[AudioClip]` op (AutoPlay on; path / `TimeRange` / source-offsets / volume / mute copied; former main soundtrack gets `Display = Background image`, `Style = Waveform`), then clear the migrated entry. Reuse the op-insertion `RecordingSession` already performs when it spawns an `[AudioClip]` during recording. Run on **save**, not load (matches `CompositionSettings.GetClips`' clean-on-load / persist-on-save idiom).
- Placement: drop the migrated op at a sensible spot (near the composition output / off to one side) so it doesn't overlap existing nodes.
- Generalise `TryGetMainSoundtrack` to union settings-list + op-flagged clips during the transition, so background waveform / FFT / export keep working before and after migration.

## Retire `IsMainSoundtrack` (G5/G6-adjacent — decompose, don't relocate)

Long-term goal (agreed 2026-07-16): drop the main-soundtrack designation entirely. The flag is documented tech debt (`TimelineAudioClip.IsMainSoundtrack` comment) bundling **three** concerns; each gets its own, different answer:

1. **BPM / beat raster — model already done, needs UI.** Per-clip `Bpm` was migrated to `Playback.Bpm` on `CompositionSettings` (`LegacyBpmForMigration` is read-once back-compat). Remaining work is settings UI only: a BPM field + **initial time offset / bar-0 anchor** (a property of the *raster*, not of any clip), plus convenience actions "adopt BPM from clip" and tap-tempo.
2. **Timeline waveform background — a `Guid` reference, not a data flag.** Replace the bool on the clip with a `Guid` in `CompositionSettings` pointing at the chosen `[AudioClip]` op (project convention: reference by Guid). Set via timeline context menu **"Set as main soundtrack"** on a selected audio clip, and via a small project-settings UI that *creates + selects* an `[AudioClip]` op. Must preserve the tier-1 experience: adding a soundtrack stays **one action** (pick file → op created, AutoPlay on, waveform Guid set — no manual wiring). Kills the `handledMainSoundtrack` scan-for-the-winner guard.
3. ✅ **FFT / audio-reactive — becomes a graph tap, no designation needed** (landed 2026-08-09, needs live test; part of the video-audio batch — see the **Review handoff** section of [`Plan_VideoAudio.md`](Plan_VideoAudio.md) for the full file inventory and verification status). Default is unchanged: with nothing wired, `[AudioReaction]` analyzes the **final mix** (react to what you hear), so all ~30 existing instances behave exactly as before. New optional `Source` (`MultiInputSlot<AudioGraphNode>`) + `Result` pass-through: inserted inline (sources → `[AudioReaction]` → bus) it declares an effect-less `FxInsert`, so the routing bus realises those sources as their own buffered submix and the FFT is read from that — the `[AudioLevel]` mechanism, extended from a level to the full FFT/band set. New Core type `ChannelAudioAnalysis` wraps a private `AudioAnalysisContext` and reads via `BassMix.ChannelGetData` (non-consuming; the plain `Bass.ChannelGetData` would steal samples from playback). Same side-branch trap as `[AudioLevel]`, with the same `IStatusProvider` warning. Note this does **not** yet resolve the global either/or: `UpdateFftBufferFromSoundtrack` still returns early unless `AudioSource == ProjectSoundTrack`, so in Live/ExternalDevice mode the *unwired* default is still the input device, not the mix.
4. **Export — full mixdown**, not a designated-clip special case. Unify on the existing mixdown machinery (`GetFullMixDownBuffer` path used for external-audio mode); removes the last consumer of the flag.

**Migration:** on load, a clip with `IsMainSoundtrack == true` sets the settings Guid; keep serializing the flag for a version or two, then remove. Sequencing: (2) can land early (it's a pointer swap); (3) and (4) depend on graph analysis/mixdown work, so the flag's *consumers* retire incrementally.

## Phases

### Phase A — `AudioReference` substrate + implicit collection (tiers 1–2)
Introduce the `AudioGraphNode`/`AudioReference` type + `TypeUiRegistry` colour. `[AudioClip]` emits an `AudioReference` (playhead-independent) and keeps its `IAudioClipProvider` handle. Implicit child-scan collection (`AudioClipCollector`) realises leaf clips at unity into the engine — coexisting with the existing settings-list playback so **nothing breaks**. Depth-1 graphs only (no combinators yet).
*Outcome:* dropping/placing an `[AudioClip]` plays via auto-collect; existing projects unaffected.

### Phase B — Settings-list migration + clip Display/Style
Migrate `Playback.AudioClips` → visible `[AudioClip]` ops on save; `TryGetMainSoundtrack` union. Add the `Display` (Clip / Background image) and `Style` (Waveform / Volume level) params, including the amplitude-**waveform** renderer TiXL currently lacks (only an FFT spectrogram exists today). `Precise` full-res-on-zoom deferred.
*Outcome:* old projects load unchanged; on save the soundtrack becomes a visible op rendered as a background waveform; audio images intact.

### Phase C — Root + combinators + BASS reconciliation (tier 3)
`GenerateAudioProcessingBlock` root; `[AudioGroup]`/`[AudioMix]` combinators; the `StructureHash`-driven diff against the live mixer. Explicit-root ownership precedence vs the implicit collector.
*Outcome:* wire clips → groups → output; bundle two clips and adjust their volumes; reroute without glitches.

### Phase D — Implicit-relationship lines + "realize"
Delivered by the separate [`Plan_ReferenceLines.md`](Plan_ReferenceLines.md) (the general graph affordance). Audio is its **first-class realize case**: the clip→root reference has a real slot pair, so a dashed line materialises into an actual `AudioReference` wire via `AddConnectionCommand` with a glitch-free ownership handoff. This plan just registers the audio matcher and supplies the realize command.
*Outcome:* a fast-and-dirty project's auto-routing is visible and becomes explicit in one click.

### Phase E — Metered control ops
`[AudioLevel]` + `[Duck]`; per-bus level exposure from the engine.
*Outcome:* duck music under a VO clip; sidechain pump.

### Phase F — Effects + multi-send
`[AudioReverb]`/`[AudioEffect]` FX inserts with tail-fade on removal; split-stream multi-send.
*Outcome:* reverb on a group; one clip sent to two buses.

### Phase G — Generalise + retire legacy
Video transitions as `VideoReference` combinators (the "2 tex + float → 1 tex" op is an SDF-style combinator); align data via the `[DataClipPlayer]` rename + AutoCollect (#1078). Delete the settings-list playback path and archive `Plan_TimelineAudioClips.md`.
*Outcome:* one reference-graph pattern across audio/video/data; legacy audio path gone.

## Open questions / deferred

1. **Single-root vs multi-send** — *Resolved:* multi-send is allowed; the AudioProcessing root owns the realisation (split streams) internally, so it's invisible to the user and can't become an accidental footgun. See §Realisation.
2. **FX backend quality.** BASS DX8/BASS_FX built-ins vs hosting something better. The architecture is agnostic (FX is a bus property); quality is a separate call.
3. **`IsMainSoundtrack` full unbundle.** `Display` takes the background-render concern; FFT-source routing and export-inclusion still need explicit homes (a connection/flag each) rather than one magic bool.
4. **Default `Display`/`Style`** for a newly-created `[AudioClip]` (Clip + Waveform likely; a dropped soundtrack might default to Background image).
5. **Reference-line feature** — *Resolved:* split into [`Plan_ReferenceLines.md`](Plan_ReferenceLines.md); this plan consumes it (audio is its first-class realize case).
6. **Video re-architecture depth.** Keep frames flowing on the wire and wrap structure in a `VideoReference` node, vs a fuller reference-on-wire rework. Don't over-unify video.
7. **Sample-accurate sidechain** (look-ahead limiter keyed off another bus) — stays structural; deferred.
8. **Sends vs export mixdown** — how multi-send interacts with the sample-accurate export path (`AudioRendering`).
9. **Spatial audio routing** — *Resolved (contract):* the reference carries a **routing kind** and spatial is `Direct` — routed to the HW-3D / output path, not a submix, so it isn't group/FX-able (confirmed: no `MixerAddChannel`, excluded from FFT/metering, separate export path). The discriminator is baked into the Phase-A contract (cheap now, a wire-shape migration later). *Deferred (feature):* an optional *software spatializer* node that pans/HRTFs into the mixer to make spatial routable at the cost of HW 3D.
10. **Event-driven sampler + polyphony** — *Resolved (safe to defer, zero contract risk):* since the reference points at "a routable channel" (§Data model), a future polyphonic op just sums its voices into its *own* internal submix and exposes that as its channel — the root routes it identically to a mono source. The event input is an ordinary `InputSlot<DataClip>`, orthogonal to the output contract. So nothing about the sampler needs baking in now; it's purely future feature work (a source op that consumes a `DataClip` event stream and spawns a voice per event — the socket the two plans create together).
11. **Loose-source audibility control (partially resolved).** Implicit playback means a *loose* source keeps playing even when it isn't evaluated — "magically active" audio with no visible cause or off-switch (the worry that also killed shadow ops).
    - ✅ **Stale-bus gating landed:** an `[AudioBus]` that stops being evaluated no longer keeps playing a frozen last state (deaf to upstream parameter changes) — buses heartbeat into `AudioBusRegistry` (`Core/Audio`) on update, and `AudioEngine.CompleteFrame` pauses submixes that miss it (~2-frame slack); the next evaluation revives them. Rule: *evaluated bus = audible + live params; non-evaluated bus = silent.* Paused submixes also stop pulling their sources' decode streams.
    - ✅ **Transport gating landed** (all graph audio, not just loose): while `PlaybackSpeed == 0` both explicit `[AudioBus]` submixes and the implicit default bus pause (a paused submix also stops *pulling* generator streams, so tone `StreamProcedure`s genuinely halt) — matching soundtrack-clip behaviour. Stop = quiet is the natural off-switch. *Deferred:* idle-motion / live-use exemption as a per-project setting.
    - Still open: (b) a per-op "sounding" **indicator** (`StatusAnimated`/`StatusAttention`) + a global **"kill all audio"** (hang on `AudioMixerManager` global mute); (c) draw the implicit source→output relationship as a **reference line** ([`Plan_ReferenceLines.md`](Plan_ReferenceLines.md)) so it's visible, not magic.

## Back-compat

Covered above: no load-time mutation, migrate on save, `TryGetMainSoundtrack` union, `[AudioClip]` op kept canonical (transitional-phase projects that already contain it stay valid). Existing playback and spectrogram/waveform images work untouched until (and after) migration.

## Manual test sets

Written 2026-08-01 (except the pre-existing clip-interaction set):

- `.tests-manual/timeline-audio-clips.md` — clip interactions (drop/drag/trim/delete) + updated legacy-migration step (startup migration, read-only packages excluded).
- `.tests-manual/audio-graph-routing.md` — bus routing, combine folds, reverb/echo inserts, FX chains, tail fades, unwire = silence, bus-to-bus handoff (+ documented multi-send limitation).
- `.tests-manual/audio-ducking-metering.md` — bus `Level`, `[AudioLevel]` tap, `[DuckAudioLevel]` incl. GainAndBias.
- `.tests-manual/audio-display-style.md` — Display Clip/BackgroundImage (clamped/aligned background, FFT designation), Style Spectrum/Waveform/VolumeLevel.
- `.tests-manual/audio-graph-export.md` — graph audio in video render (params/duck baked in, live state intact after), executable export (soundtrack dialog, graph audio, end-of-timeline).
- Still pending: `audio-realize.md` once reference lines (Phase D) land; a compressor-focused step once `[AudioCompressor]` is verified by ear.
```
