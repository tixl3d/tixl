# Recording

Which sources the timeline's Record button captures.

- **Capture Audio** — record the active audio input device while the session runs.
- **Capture IO** — record incoming **MIDI** and / or **OSC** events into a data file alongside the audio clip, so a live take can be replayed and refined later.
