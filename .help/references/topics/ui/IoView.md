A window that shows recorded live-input events — MIDI and OSC — as a data clip on a small timeline.

Use it to capture a live controller or performance feed as timed events, then replay that recording back in the studio to craft visuals against real performance data instead of tweaking by hand. It's about reusing a captured live set, not audio production. You'll find the recorded events drawn as a track you can scrub alongside your composition.
