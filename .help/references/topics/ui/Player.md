The standalone runtime that plays an exported TiXL project outside the editor.

It runs a project on its own, without the graph, parameter panels or timeline around it — the form your work takes when you ship it as a self-contained executable. You build one with the [ui:PlayerExporter|Player exporter]. Use it to deliver a finished piece that runs on a machine that doesn't have the editor installed.
