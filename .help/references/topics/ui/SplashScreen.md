The image shown while TiXL starts up, before the main editor appears.

It displays the TiXL logo along with the version number and a loading status message that updates as the editor compiles operators and loads your projects. It isn't interactive and closes on its own once the editor is ready. If startup ever stalls, the status line tells you which step it reached.
