---
video: F8J8w-JK3hA
type: meetup
date: 2026-08-10
title: TiXL Meetup 2026-08-10 — Drone Show Choreography, Video Pipeline & Audio Graph
duration: 5:32:39
---

A five-and-a-half-hour community meetup that opens with reaction-diffusion feedback experiments, previews the upcoming 4.3 features (asset link files, the node-based audio graph, video editing timeline, FFmpeg playback and HAP export, texture-array frame history), and ends in a long collaborative session building a 100-drone show in TiXL with speed/collision validation and a DDS texture export into Blender.

## Mentions
- 5:25→7:25 [RadialPoints] [ToneMapping] · passing · experiment · Example · 50% — Briefly named while poking through a physarum agent setup: a million radial points seed the agents, and a tone-mapping pass helps reveal structure in over-bright feedback textures.
- 8:05→11:15 [Sharpen] [Blur] [Displace] [HasTimeChanged] · explained · experiment · Concept · 80% — Build reaction-diffusion as a pure feedback loop where sharpening is the essential ingredient (blur and displace only shape it), and gate the re-initialization with the DidRewind mode of [HasTimeChanged] so rewinding restores the seed image.
- 11:15→12:05 [FastBlur] · explained · experiment · Tip · 80% — Prefer it over plain [Blur] inside feedback loops for smoother edges, and switch the texture format to 32-bit so repeated blur/sharpen passes don't accumulate artifacts; the trade-off is less precise control over the blur kernel.
- 12:05→14:06 [Sharpen] · explained · experiment · Parameters · 80% — Balance sample radius against the upstream blur amount to steer the emerging pattern: too little blur collapses the feedback into hard chip-design-like structures, and the built-in clamping strongly hardens the result.
- 14:06→15:55 [RadialGradient] · passing · experiment · Tip · 55% — Used as a mask driving effect strength so one image becomes a spatial parameter sweep, helping to spot which blur/sharpen ratio produces the sought-after dot distribution.
- 16:03→17:50 [DirectionalBlur] · explained · experiment · Example · 75% — Feeding a texture into its vector input steers the blur direction per pixel, which inside a feedback loop starts aligning the emerging pattern along the guide image.
- 17:51→19:30 [Text] [BlendImages] · passing · experiment · Example · 70% — Quickly rendered a date title into a feedback chain by blending a text layer over the simulation so the growing pattern consumes the lettering.
- 21:23→23:35 [ShardNoise] [DirectionalBlur] · explained · discussion · Example · 80% — Piping it into [DirectionalBlur]'s FX-texture input both directs the blur and displaces the diffusion toward the noise gradient, warping the pattern into flowing organic streaks.
- 25:36→30:25 [ui:OutputWindow] · in-depth · discussion · Concept · 80% — Planned redesign: selecting an output opens an infinite zoomable 2D canvas where content slices, surfaces, projectors and reference images are laid out in physical units, so projection resolution and requested render size can be inferred from real-world slice dimensions.
- 30:25→31:45 [SpoutOutput] [NdiOutput] · passing · discussion · Tip · 70% — Named as routing targets of the upcoming output-mapping setup, which should remove today's need to keep several output windows open just to keep multiple Spout/NDI streams updating.
- 32:10→34:10 [ui:Timeline] · explained · discussion · Concept · 75% — A nested operator can host a full editing-style layout of clips, audio and time-retiming, effectively turning a composition into a lightweight video editor you can cut through.
- 34:20→39:05 [ui:Asset] · in-depth · discussion · Concept · 85% — Dropping a folder offers three choices — copy into assets, sort by type folders, or create a small link file — and the link file stores an ID plus multiple per-machine target paths so the same project finds gigabytes of external footage across different computers or drives without copying.
- 39:07→42:45 [PlayAudioClip] [ui:ShaderGraph] · in-depth · discussion · Concept · 80% — Playback moved from fire-and-forget commands (which kept sounding after the operator stopped being evaluated) to a proper audio graph assembled like the shader graph, so combining, fading and effect-chaining sounds now follows evaluation correctly.
- 42:47→44:20 [ui:Timeline] · explained · experiment · Tip · 75% — Drop an audio operator into it to get a clip with a switchable background mode — the answer to running more than one background soundtrack — plus display options for waveform, spectrum, or volume level.
- 44:20→48:06 [AnimBoolean] [AdsrEnvelope] [RaymarchField] · explained · experiment · Example · 60% — Trigger a one-shot sample by wiring a boolean animation into the play gate and shape it with attack/sustain/release; effect nodes like echo run as C++ in the playback base library, making the audio graph a kind of [RaymarchField]-for-audio.
- 48:39→53:30 [ui:LocalTime] [ui:Timeline] · in-depth · discussion · Concept · 80% — Nested operators with their own local time now genuinely work as reusable animated clips ("pre-comps") you can stretch, cut and remap in the parent timeline — including slip-editing by moving the source — though keyframes still display against remapped time, which reads confusingly.
- 55:54→57:38 [PlayVideo] · explained · discussion · Example · 80% — Drive its playback-time parameter directly with animations or noise to jump non-linearly through a file — the intended pattern for VJ-style random-access playback rather than timeline-synced clips.
- 57:38→59:06 [VideoClip] · explained · discussion · Example · 75% — Dragging a video into the timeline yields a clip locked to playback time for frame-synced work, and clips can now carry their own keyframes for colorizing, fading and cross-fades.
- 59:06→1:01:45 [PlayVideo] · explained · discussion · Performance · 85% — For 4K-plus material switch OptimizedFor from fast seeking to playback performance, which skips frame caching and decodes on the GPU with a GPU-to-GPU copy (no CPU readback); with FFmpeg the seek position is also frame-precise now, unlike the old Media Foundation path.
- 1:03:05→1:05:40 [FilesInFolder] [PickStringFromList] · explained · experiment · Example · 70% — List a linked folder's contents and pick one path into the video player to build a clip switcher; the file list needed a manual trigger update before it populated through the new folder reference.
- 1:05:40→1:08:00 [PlayVideo] [StopWatch] · explained · discussion · Performance · 80% — Seek cost grows with the codec's group-of-pictures length and how deep into the file you land, so feed a running seconds value into playback time to start clips from zero, or re-encode to HAP/ProRes when you need instant scrubbing.
- 1:08:24→1:09:30 [ui:RenderSettings] · explained · discussion · Parameters · 80% — Besides custom/loop/soundtrack ranges there is now a Continuous source range that records from the current time until you press cancel, handy for capturing improvised runs.
- 1:09:30→1:17:10 [ui:RenderSettings] · explained · experiment · Gotcha · 75% — HAP export fails with a cryptic FFmpeg error unless the container matches: it must be written as .mov, and changing the codec doesn't auto-update the file extension in the output target, so fix the extension by hand.
- 1:20:29→1:23:55 [KeepInTextureArray] · explained · experiment · Concept · 80% — Give it a slice count (e.g. 128) and advance the target index each frame to turn live video into a ring-buffer history of frames; changing the array size restarts the fill from scratch.
- 1:23:55→1:30:15 [CustomPixelShader] [KeepInTextureArray] [LinearGradient] · in-depth · experiment · Example · 80% — Declare a Texture2DArray in the additional-defines on the same t-register as an image input, sample it with float3(UV, slice), and drive the slice from a gradient image to displace pixels through time — brighter areas reach further back into the frame history.
- 1:30:15→1:33:20 [ColorGrade] [ConvertFormat] · explained · discussion · Gotcha · 75% — Watch texture formats before a texture array: inserting a color grade silently promotes the stream to 16-bit, doubling array memory and bandwidth, so insert a format conversion back down to 8-bit before storing history frames.
- 1:37:43→1:39:15 [TimeDisplace] · passing · discussion · Comparison · 70% — Already implements the gradient-driven frame-history warp as a ready-made shader, so check it before hand-rolling the same effect from a texture array.
- 1:39:30→1:43:35 [Once] [CountInt] [StopWatch] [CompareInt] · explained · experiment · Example · 75% — Pre-warm a frame-history array at project start: fire a single trigger that resets a counter and a seconds stopwatch together so the video restarts from zero, then compare the counter against the slice count to stop writing once the buffer is full.
- 1:43:37→1:44:35 [TriggerAnim] [AnimValue] · passing · discussion · Tip · 70% — Cited for their end-of-run trigger outputs as the pattern a counting operator could adopt to announce when it wraps, instead of pairing it with an explicit compare.
- 1:44:35→1:46:30 [CountInt] · explained · experiment · Tip · 70% — Disable its modulo wrap when using it as a write index that must eventually stop — counting past the array size lets the compare-gate cleanly halt writes — and add a time offset (minutes × 60) to start sampling mid-video past boring intros.
- 1:46:31→1:53:00 [CustomPixelShader] [PlayVideo] · explained · experiment · Example · 65% — Sampling two nearby slices of a frame-history array and taking the absolute RGB difference crudely detects inter-frame motion (too noisy with fast movement), and the video player's frame-changed output can gate writes so duplicate frames don't pollute the history.
- 1:59:48→2:02:30 [ui:ProjectSettings] [ui:Timeline] · explained · answer · Concept · 75% — The background soundtrack moved out of the composition settings onto the timeline itself: drag the audio in, set it to background mode (or the locked background-image display), and use a negative clip offset instead of the removed soundtrack-offset setting.
- 2:03:35→2:04:10 [BlendPoints] · passing · discussion · Tip · 70% — Wished-for upgrade: accept any number of point-set inputs and crossfade through them, instead of chaining pairwise blends for multi-scene transitions.
- 2:04:12→2:05:08 [DrawPoints] [Camera] [RenderTarget] [Bloom] · passing · experiment · Example · 70% — Briefly wired as the standard viz stack — points through a camera into a render target with bloom and a touch of camera shake — to stand in for drones against a night sky.
- 2:05:10→2:06:45 [CommonPointSets] [DrawLines] [Group] · passing · experiment · Example · 65% — Used to block out a stage: a cube point set drawn as lines becomes the allowed flight-zone cage that the rest of the scene is grouped into.
- 2:06:48→2:11:55 [RaymarchField] [CustomSDF] [WriteToFile] · explained · answer · Gotcha · 70% — To detect externally when an SDF finished compiling, grab the raymarcher's second output (the generated shader-code string) and write it to a file as a completion signal — though file writing currently fails outside permitted paths under the new asset pipeline, logged as a to-do.
- 2:12:00→2:14:35 [DrawMesh] · passing · experiment · Tip · 65% — Sanity-check imported OBJ scale by placing a known-size reference cube alongside; a scene modeled in meters that sits correctly on the floor saves guessing at flight-space dimensions later.
- 2:14:37→2:16:10 [DrawPoints] · passing · experiment · Example · 60% — Enlarged a set of exactly 100 points and rotated them into place as a drone fleet hovering above blocked-out buildings.
- 2:17:30→2:18:25 [AudioPlayer] · passing · experiment · Tip · 55% — Named as the simpler alternative for soundtrack playback while the new audio-bus route was chosen for finer control.
- 2:18:25→2:20:05 [RadialPoints] · passing · experiment · Example · 60% — Animated very slowly from a small radius as an opening drone formation, with per-point twinkling added via a random phase.
- 2:20:08→2:22:50 [Round] [ui:ProjectSettings] · explained · discussion · Concept · 65% — Emulate a 10 FPS device by quantizing animation time with a rounding step, and the discussion spawned a feature idea: a project-level frame rate the timeline would show as vertical bars so you notice when you animate faster than the hardware can update.
- 2:23:00→2:26:06 [LoadImage] [DetectEdges] [PointsOnImage] · explained · experiment · Example · 70% — Chain edge detection into image-based point scattering to turn a logo into an exact number of device positions; for logos the filled center reads better than the outline, and unlit "black" drones cost nothing.
- 2:26:06→2:28:30 [TransformPoints] [BlendPoints] · passing · experiment · Example · 60% — Transformed and rotated extracted logo points into the flight volume before crossfading the fleet toward them.
- 2:32:33→2:47:00 [CustomPointShader] [PointsToCPU] · in-depth · discussion · Example · 85% — The output points buffer isn't cleared between frames, so reading the result buffer at the same index yields last frame's position for free: subtract, multiply by the frame rate for meters-per-second, split horizontal (7 m/s) and vertical (3 m/s) limit checks, and write a fail attribute that recolors offending points.
- 2:47:00→2:52:30 [CustomPointShader] [DrawMeshAtPoints] · explained · experiment · Example · 75% — A brute-force for-loop over all source points (skipping the own index via continue) flags pairs under a 2 m separation minimum, and instancing 1 m spheres at the points makes the safety radius visible at a glance.
- 2:57:40→2:59:45 [LoadSvg] [ResampleLinePoints] · passing · discussion · Tip · 55% — An SVG outline resampled to exactly the fleet size gives evenly spaced logo targets, noticeably better than scattering points on a bitmap.
- 3:01:04→3:02:00 [CollisionForce] · passing · discussion · Gotcha · 60% — Recalled from earlier experiments as working but prone to bouncy, springy motion between colliding points.
- 3:02:51→3:06:30 [ParticleSystem] [ReconstructiveForce] [HasTimeChanged] · explained · experiment · Example · 80% — Emit the fleet once, pull it toward target points with a reconstructive force, and reset-plus-re-emit on rewind detection; the emit count must exactly match the target point count or particles converge on the wrong spots.
- 3:06:30→3:13:20 [CollisionForce] · in-depth · experiment · Parameters · 80% — It reads each point's scale as its collision radius, so set point size to the real safety sphere; the cell size must be large enough to catch neighbors (around 5 worked), and flipping the response sign turns repulsion into attraction.
- 3:13:27→3:16:00 [CustomPointShader] · passing · experiment · Tip · 70% — Color-coded failure states in the shader — red for collisions, orange for overspeed, pushed into HDR — so violations flash visibly during playback instead of hiding in numbers.
- 3:17:47→3:19:55 [DrawLines] · passing · experiment · Tip · 70% — Drawing a line from each target to its simulated point reveals exactly how far collision avoidance displaces the formation.
- 3:19:56→3:22:30 [ReconstructiveForce] [CollisionForce] · explained · discussion · Concept · 80% — The two fight each other: push hard toward targets and collisions persist, push gently and the shape dissolves — so tune reconstruction strength against collision resolve, and enlarge point scale so avoidance spheres clear earlier.
- 3:22:37→3:24:30 [PointSimulation] · passing · experiment · Gotcha · 65% — Added for velocity damping, but it slowed the collision avoidance as much as the motion itself, making violations worse rather than better.
- 3:27:10→3:29:30 [BlendPoints] [RadialPoints] · passing · experiment · Example · 65% — Chained a second crossfade to a spiral scene built by giving a radial point ring a vertical offset and several extra rotations.
- 3:33:09→3:40:00 [CollisionForce] · in-depth · discussion · Concept · 85% — Internally it builds a spatial hash grid, computes the gap from both points' scaled radii, and displaces positions directly (not via velocity), applying each pair only once by index order; the resolve parameter is the per-frame push-out speed, so lowering it trades slower separation for less overspeed flicker.
- 3:40:15→3:44:50 [CustomPixelShader] · explained · discussion · Example · 75% — Bake a full point animation for external tools by writing one texture row per frame — positions in the first pixels, colors after — starting from the points-to-texture example; the resulting float texture is only a few megabytes.
- 3:48:12→3:53:30 [IntToFloat] [SlidingHistory] · passing · experiment · Example · 60% — Fed the frame counter through an int-to-float conversion into the baking shader as the row index; a sliding-history operator exists, but writing rows explicitly kept the export layout under control.
- 3:57:52→3:58:45 [RandomizePoints] · passing · experiment · Gotcha · 60% — Its per-point color randomization caused constant glitter through a crossfade, so it was pulled out and re-inserted after the blend where its effect could be dosed.
- 4:12:40→4:13:15 [Bloom] · passing · experiment · Concept · 60% — A RenderDoc capture shows it as a stack of progressively downsampled additive passes recombined at the end — a useful mental model when hunting for your own draw call in a frame capture.
- 4:49:00→4:49:40 [CustomPointShader] · passing · discussion · Tip · 60% — Exposing a gradient plus a blend-amount parameter lets one animated float fade an entire fleet's color scheme in and out.
- 5:05:13→5:05:30 [SpherePoints] · passing · discussion · Tip · 55% — Suggested as a calmer opening formation than a ring for a morphing point choreography.
- 5:29:37→5:30:06 [CustomPointShader] · passing · discussion · Gotcha · 60% — Disable color-coded warning overlays before baking an export, otherwise the QA colors end up in the final output data.
