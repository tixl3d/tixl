---
video: LLpl6QEL7bo
type: meetup
date: 2025-08-26
title: TiXL Meetup 2025-08-25 — Timeline, PBR-rendering, particles, fields and the future of outputs
duration: 5:15:59
---

A long maintainer-led meet-up walking the last two weeks of commits: timeline/time-clip editing, beat-locking, the unified PBR rendering path, field-driven point selection, and an extended brainstorm on a future multi-output / projection-mapping system.

## Mentions
- 0:10:30→0:13:20 [ui:Timeline] · explained · discussion · Concept · 80% — Time clips structure a linear edit; only clips actually wired to the active output render, and unconnected ones fade out as a warning that they have no effect.
- 0:13:20→0:15:20 [ui:Timeline] · explained · scripted · Tip · 80% — New time clips auto-stack to avoid overlap, and Ctrl+X splits a clip at the playhead while re-wiring the new half into the same group — much faster than hand-editing overlapping clips.
- 0:16:40→0:21:30 [ui:OperatorSettings] · explained · scripted · Tip · 75% — Per-layout you can hide individual editor tabs and the hidden state is stored implicitly with each saved layout (F1/F2), giving a clean stage layout you toggle into.
- 0:23:40→0:25:50 [Camera] · explained · scripted · Example · 75% — Inject a second camera, rename it, and switch the output from auto to that named camera so you can fly an "edit camera" around a pinned background scene without disturbing the final composite camera.
- 0:26:40→0:28:40 [VideoDeviceInput] · explained · answer · Concept · 80% — This is the webcam/capture-card operator (anything Windows exposes as a DirectX video device); its device list is the awkward part, currently pulled in via OpenCV.
- 0:36:00→0:41:10 [ui:AudioInput] · in-depth · experiment · Concept · 80% — Tap a beat then let auto beat-locking continuously nudge BPM and phase toward detected snares/kicks, so a VJ no longer has to keep tapping; press the resync key on a measure to relock if it drifts.
- 0:35:40→0:36:00 [ui:Settings] · passing · scripted · Tip · 60% — Audio volume can be pushed above 1.0 (up to 1100%) to make playback louder than the source.
- 0:38:40→0:40:40 [AnimBoolean] · explained · scripted · Example · 75% — A tidier on-beat trigger than wiring AnimValue by hand; drive it from the kick and feed its pulse into downstream animation. Internally identical to AnimValue but readable in the graph.
- 0:41:30→0:43:40 [ui:Settings] · explained · answer · Parameters · 75% — A beat-sync offset adds a fixed lead time so playback runs earlier, compensating for LED controllers or devices with large output latency (e.g. half a second).
- 0:43:40→0:46:20 [AnimInt] · explained · answer · Comparison · 70% — Same internals as AnimBoolean's counter but without the on-beat UI; the request raised here is to give it a modulo wrap and a speed factor so it counts within a range and loops.
- 0:50:00→0:55:00 [SetSpeedFactors] · in-depth · scripted · Example · 80% — Pin it at the start of a chain to override the global time factors A and B; map a MIDI knob to a factor and everything time-driven downstream speeds up or slows together for live tempo ramps and breakdowns.
- 0:52:40→0:53:20 [DetectEdges] · passing · experiment · Tip · 60% — Dropped in with mirror-repeat wrapping to dress up a flat output into something more interesting on the fly.
- 0:53:00→0:54:10 [RenderTarget] · explained · scripted · Performance · 70% — Piping into a 32-bit (float) render target instead of 8-bit makes a large visible quality difference for accumulated/feedback-style effects.
- 0:54:00→0:58:00 [SetSpeedFactors] · explained · discussion · Example · 75% — Two independent speed factors let one element race while another crawls; map factor A and factor B to separate MIDI inputs to layer fast and slow motion that still lock to the same measure.
- 0:55:40→0:58:00 [KeyboardInputAsInt] · explained · discussion · Tip · 70% — Mapping number-row keys to an integer and feeding that into the time factors lets you punch a speed multiplier live with the keyboard during a set.
- 0:56:40→0:59:20 [ForwardBeatTaps] · explained · answer · Concept · 65% — Forwards externally-tapped beats (e.g. an OSC tap from an iPad, or an Ableton click track sending one pulse per beat) into the beat system so someone else can tap for you.
- 1:01:00→1:03:30 [PlaybackFFT] · explained · experiment · Example · 70% — Feed the playback FFT into a texture history to scroll the spectrogram over time; the basis for sliding-window beat detection that compares each beat's pattern to the previous.
- 1:01:40→1:04:00 [Sketch] · passing · experiment · Tip · 60% — Used as a quick freehand scratch surface on the output to illustrate ideas live; cheap to drop in and draw on.
- 1:10:40→1:13:20 [ui:Graph] · in-depth · scripted · Tip · 80% — Horizontal snapping (Alt+A) aligns operators so the connection-line router knows they're related and keeps parallel wires from overlapping — subtle but it cleans up tangled graphs.
- 1:12:20→1:18:00 [RadialPoints] · explained · discussion · Parameters · 75% — Unlike LinePoints or GridPoints it lacked a per-point color; the F1/F2 factor-plus-offset pair (also discussed here) drives a value range along the radius that turned out useful for many setups.
- 1:15:40→1:19:00 [RandomizePoints] · explained · scripted · Example · 75% — Animate the random phase to keep points churning; pair with a color randomization for saturation/brightness jitter across the set.
- 1:16:20→1:19:00 [TriggerAnim] · explained · scripted · Example · 70% — Feed its pulse into a randomized phase so each beat re-rolls point positions; cleaner than wiring AnimValue for the same job.
- 1:18:00→1:20:00 [Accumulator] · explained · scripted · Example · 75% — Continuously adds a per-beat amount into a value to advance an animation phase step-by-step; reach for it when you want monotonic progress rather than an oscillation.
- 1:18:40→1:19:00 [ui:InfinitySlider] · passing · scripted · Tip · 70% — Hold right mouse to drag a value field around without changing its value, so you can escape a corner you've locked into.
- 1:19:30→1:24:00 [Accumulator] · in-depth · discussion · Gotcha · 80% — Endlessly accumulating a phase eventually loses float precision (visible past ~100k points or when adding large per-frame amounts); the clean fix is to cross-blend between two RandomizePoints toward a zero-phase copy rather than snapping the phase back.
- 1:30:00→1:33:20 [ui:ColorEditor] · explained · scripted · Tip · 75% — A small HDR badge appears once a color's brightness exceeds 1 (Ctrl-drag to push it there); the on-hover mini interpolation curve shows how a gradient blends, exposing OKLab's odd dips through zero.
- 1:33:20→1:36:20 [OrbitCamera] · explained · scripted · Gotcha · 75% — Rotating with the middle mouse used to silently change the distance between camera position and target, breaking saved camera animations; a fix keeps that distance fixed while orbiting.
- 1:35:00→1:36:00 [LoadObj] · passing · scripted · Example · 65% — Standard scene scaffold: LoadObj into DrawMesh under a Camera as the starting point for a 3D setup.
- 1:36:20→1:38:00 [LoadObj] · explained · scripted · Tip · 70% — Restricting a path parameter's file filter (here to .obj) makes the resource picker show only matching files; many operators were missing these filters and returned noise or nothing.
- 1:39:30→1:44:00 [PointLight] · in-depth · scripted · Comparison · 80% — Prefer it over SetPointLight: it wraps the same setup but also draws a gizmo sphere you can grab and move in the viewport; the specular term was previously computed from the wrong light position and is now corrected.
- 1:40:40→1:41:40 [SetPointLight] · explained · discussion · Concept · 70% — The lower-level light operator PointLight builds on; usable directly but lacks the gizmo, and its position feeds the now-fixed specular computation.
- 1:42:40→1:43:20 [SetEnvironment] · passing · scripted · Tip · 65% — Switch to the new "black" environment preset to isolate and inspect a single light's direct contribution with no ambient wash.
- 1:45:00→1:53:00 [DrawMesh] · in-depth · scripted · Example · 85% — DrawMesh now accepts a field: convert an SDF/plane field through SdfToColor and feed it as the color input to paint a procedural color map onto mesh surfaces, consistent with the SDF render path.
- 1:46:20→1:48:00 [RaymarchField] · explained · discussion · Gotcha · 75% — Shares the unified light/environment so an SDF torus matches a mesh torus, but it does not support normal mapping yet — keep normal maps off that branch.
- 1:47:40→1:53:20 [SdfToColor] · explained · scripted · Example · 75% — Turns a distance field into a color you can route into DrawMesh, RepeatMeshAtPoints, or DrawMeshAtPoints so every PBR path colorizes from the same field identically.
- 1:46:40→1:48:40 [SpreadLayout] · explained · scripted · Tip · 75% — Spreads connected inputs evenly in space, auto-spacing more steps as you add more connections — handy for laying out parallel test setups side by side.
- 1:52:00→1:53:40 [RepeatMeshAtPoints] · explained · experiment · Performance · 65% — Repeats a mesh at each point but overdraw gets expensive fast with overlapping tori; reduce density to recover frame rate.
- 1:53:00→1:54:20 [DrawMeshAtPoints] · explained · scripted · Example · 70% — Another consumer of the shared color field: scale instances down and drive their surface color from the same field as the rest of the scene.
- 1:54:40→1:57:00 [ui:Graph] · explained · scripted · Concept · 70% — Image-comparison unit tests render convoluted scenes and flag a failure when output drifts; run them before each commit to catch rendering regressions across the PBR paths.
- 1:57:00→1:59:10 [PointsOnMesh] · explained · scripted · Example · 70% — Scatter shaded point sprites (not spheres) across a loaded mesh's surface; the close-up fade-out seen here was flagged as a bug to fix.
- 2:03:40→2:07:10 [DelayTriggerChange] · in-depth · scripted · Example · 80% — Holds a one-frame trigger open for a set duration so a particle emitter fires across several frames instead of one; also debounces rapid double-triggers down to a single emission.
- 2:05:00→2:06:20 [ParticleSystem] · explained · experiment · Example · 65% — Emit once per beat by gating emission with a beat trigger; widen the held trigger window to emit a burst of frames rather than a single instant.
- 2:06:20→2:07:40 [DelayBoolean] · explained · scripted · Comparison · 65% — Distinct from DelayTriggerChange: it delays a boolean by a number of frames rather than holding a trigger open, useful for staggering events.
- 2:07:10→2:11:00 [SetFloatVar] · in-depth · scripted · Example · 80% — Place it first inside an Execute, name the variable, and share one value across many effects; hovering shows highlighted lines to every reader of that variable.
- 2:09:50→2:11:00 [GetFloatVar] · explained · scripted · Example · 75% — Reads a named float set elsewhere with auto-completion from context, letting you reuse one driving value (e.g. an intensity) anywhere in a large project.
- 2:11:40→2:13:40 [SelectPointsWithSDF] · in-depth · scripted · Example · 85% — Feed a CustomSDF in and write the selection to an FX attribute; that attribute then drives anything per-point — here emit speed from a grid into a ParticleSystem, varying by region of the field.
- 2:13:00→2:14:40 [GridPoints] · explained · discussion · Parameters · 80% — The default changed to cell mode (fixed spacing between points) rather than bounds mode (a fixed outer extent filled by the count); GridPointsOld preserves the old default for existing projects.
- 2:15:40→2:18:40 [ParticleSystem] · in-depth · scripted · Parameters · 80% — The new Emit Velocity Factor scales launch speed; switching emit mode from sequential to lines lets DrawLines render the trails as shaded ribbons.
- 2:17:40→2:18:40 [TurbulenceForce] · passing · experiment · Example · 65% — Added to a particle setup to introduce visible swirl and structure into otherwise straight emission.
- 2:19:00→2:21:40 [SphereSDF] · explained · experiment · Example · 70% — Used as a simple animated field to drive a per-point selection factor; render it alongside via VisualizeFieldDistance to see exactly where the selection falls.
- 2:21:40→2:23:40 [CombineFieldColor] · passing · experiment · Gotcha · 60% — Reached for to mix a field with a FractalNoise field; the maintainer notes this slot expects a color/factor, not arbitrary fields, so use it deliberately.
- 2:24:40→2:36:00 [SelectPointsWithSDF] · in-depth · scripted · Parameters · 85% — The range mapping modes (centered / from-start / ping-pong / repeat) plus gain-bias decide how a field's distance maps onto the 0..1 selection; zero distance reads as maximum selection, and the strength factor lets a second selection localize the effect.
- 2:34:40→2:37:00 [MapPointAttributes] · explained · scripted · Example · 75% — Map a selection attribute (F1) to color so a hand-crafted field paints specific regions of a particle effect — e.g. turn one zone blue.
- 2:37:40→2:38:40 [PointsOnImage] · explained · experiment · Example · 65% — Seed point positions from an image (or a PlayVideo frame), turning any texture or moving effect into a point field for downstream selection and drawing.
- 2:21:20→2:23:00 [VisualizeFieldDistance] · explained · scripted · Tip · 70% — Drop it in to actually see a field's distance bands while tuning a selection range, instead of guessing where the gradient sits.
- 2:38:00→2:41:00 [PointSimulation] · explained · discussion · Comparison · 70% — The pre-ParticleSystem operator; still genuinely useful as a per-point damper, so the proposal is to rename it (e.g. DampPoints) rather than retire it, since ParticleSystem now covers its old particle role.
- 2:41:00→2:48:40 [DrawConnectionLines] · explained · answer · Gotcha · 70% — Vertically-stacked operators fall back to a spline connection that can't be hovered or disconnected; this top-down wiring case is unfinished, which is why some wires won't detach.
- 2:48:40→2:53:40 [MeshFacesPoints] · explained · scripted · Parameters · 75% — Re-implemented onto the new colored-point format with FX scale/stretch; a new option scales each point sprite by its polygon face area (computed trivially from the face's three vertices), great for spikes or cylinders that grow with face size.
- 2:54:20→2:57:20 [MoveToSDF] · in-depth · scripted · Example · 80% — Distributes grid points onto an SDF surface (e.g. a smooth-combined sphere "monkey"); move the source points and they redistribute, then draw a mesh at each to clad the shape.
- 2:54:40→2:56:00 [CombineSDF] · passing · experiment · Example · 60% — Smooth-combine several SphereSDFs into one blobby shape used as the target surface for point placement.
- 2:57:20→3:03:00 [RaymarchField] · in-depth · discussion · Performance · 80% — Cost scales with empty marching distance — it gets slower as the object shrinks in frame because rays hit the max step count; capping max distance (and min distance) would be the big optimization.
- 3:01:20→3:04:00 [ui:Graph] · explained · scripted · Performance · 75% — A fully static subtree caches and costs nothing; selecting an operator invalidates it (params might change) and breaks the cache, so deselect to let scenes settle.
- 3:03:40→3:08:00 [MoveToSDF] · explained · experiment · Parameters · 70% — Its amount factor (drivable per-point from an FX attribute via SelectPointsWithSDF) and surface-offset mode control how far points pull onto the surface; step count and max distance trade quality for speed.
- 3:08:00→3:11:00 [PointTrail] · explained · scripted · Concept · 75% — Records past point positions to draw motion trails; reworked to be more consistent and to avoid gaps in the lines, though its interpolation is still not perfectly smooth.
- 3:09:40→3:11:20 [PointTrailFast] · explained · discussion · Gotcha · 75% — Uses a cyclic buffer so it's fast, but DrawLines can't tell where the buffer wraps, leaving one persistent gap in the trail line.
- 3:10:40→3:13:40 [DrawLines] · explained · scripted · Tip · 80% — A new fade-out-if-too-long length cutoff gives a near-free Plexus-style effect: scatter PointsOnMesh, raise the count, and hide long connections so only short links between nearby points survive (also culled cheaply via alpha).
- 3:13:40→3:18:40 [LoadSvg] · explained · experiment · Gotcha · 70% — Loads SVG paths but doesn't close loops or fill, and the underlying standard is huge — treat it as line data, animate its UV/transition-progress, and expect to export SVGs cleanly rather than relying on full coverage.
- 4:03:40→4:07:00 [Execute] · explained · answer · Comparison · 75% — Use it over a Group when you only need to update/draw side-effects (set variables, run OSC output) with no data output — slightly faster since it skips the Group's transform/color overhead.
- 4:03:40→4:06:40 [SwapTextures] · explained · discussion · Gotcha · 70% — An execute-then-pass-through texture operator (an "update before output" workaround) that runs side effects first and then outputs an already-rendered buffer without re-rendering it.
- 4:48:40→4:50:00 [TransformMeshUVs] · explained · scripted · Gotcha · 70% — Its pivot is now centered so scaling grows from the middle; this is a breaking change from the old corner pivot, so older projects may shift.
- 4:48:00→4:49:00 [PlaneSDF] · passing · scripted · Gotcha · 70% — The up/down axis was flipped to fix a sign bug — a breaking change, so projects relying on the old orientation need updating.
- 4:50:00→4:52:40 [SdfToVector] · explained · scripted · Concept · 70% — Returns a position in space near an SDF surface (computed from the field normal) so you can turn a distance field into a directional/vector field for forces.
- 4:50:40→4:54:00 [VectorFieldForce] · explained · experiment · Example · 70% — Drives particles along a field's computed normals; pair with TransformField to reorient the field's vectors, not just its space.
- 4:53:00→4:54:40 [TransformField] · explained · scripted · Parameters · 70% — A new checkbox rotates the field's stored vectors along with the space when you rotate a vector field, otherwise direction and space disagree.
- 4:57:20→4:59:40 [RandomizePoints] · explained · answer · Parameters · 80% — A new uniform-randomization mode spreads points evenly (a regression that previously couldn't be done); note its Scale knob now writes to point stretch, while W stays free of scaling by default.
- 5:00:20→5:01:20 [FieldVolumeForce] · explained · scripted · Parameters · 70% — Now superset of FieldDistanceForce, with reflect-on-collision and apply-color-on-collision options that sample the SDF's color onto colliding points.
- 5:01:20→5:08:00 [BuildGradient] · in-depth · scripted · Example · 80% — Builds a gradient procedurally from a color list plus an overridable position list — feed a PlaybackFFT (or any float list) into the positions to make an audio-reactive gradient for displacement and color.
- 5:04:40→5:06:40 [SampleGradient] · explained · scripted · Parameters · 70% — A new interpolation-override lets you force linear vs. spline blending instead of inheriting it from the gradient type, decoupling output interpolation from the source.
- 5:01:40→5:05:00 [ColorsToList] · explained · scripted · Tip · 65% — Builds a reusable color palette as a list you can feed into BuildGradient and rearrange, rather than hand-wiring individual colors.
- 4:35:40→4:39:40 [ui:EvaluationContext] · in-depth · discussion · Performance · 80% — Effects should cache to a static image when nothing animates; connecting any animated parameter (even from a static gradient that's being dragged) breaks the cache and forces per-frame re-render, so watch for effects that fail to fade-out (the cached indicator).
- 4:28:40→4:35:40 [ui:Graph] · explained · answer · Concept · 75% — Node text blurs when zoomed because ImGui draws every glyph as a textured quad from a fixed font atlas (only four font sizes); a future 2D-SDF font shader would make it crisp.
- 3:20:00→3:46:00 [ui:OutputWindow] · in-depth · discussion · Concept · 80% — Extended design discussion: replace single-output pinning with a project-level list of outputs (display / Spout / NDI / virtual canvas), routable live (e.g. Ctrl+1/2) and arrangeable on a UV canvas for projection mapping, while keeping the strict output-first evaluation order intact.
- 3:46:00→3:52:00 [SpoutOutput] · explained · discussion · Gotcha · 70% — Needs B8G8R8A8 format, so a ConvertFormat sits in front today; the request is to have it auto-convert internally via a compute shader like PlayVideo does.
- 4:19:40→4:25:00 [SpoutInput] · explained · experiment · Example · 65% — Round-trips frames out to a host like NestDrop and back, letting you borrow that host's effect library and recompose the result in TiXL.
- 4:45:40→4:46:40 [RaymarchField] · passing · discussion · Gotcha · 55% — A community SDF-on-the-fly setup loads SDFs as JSON nodes and must be wired into a RaymarchField to show anything; flagged as needing cleanup and documentation.
