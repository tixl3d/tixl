---
video: 2X8LyBvB8H0
type: meetup
date: 2026-06-29
title: TiXL Meetup 2026-06-29 — Particles Deep Dive
duration: 4:06:34
---

A four-hour community meetup centered on the particle system: why transparent particles get black outlines and how depth sorting works, the ring-buffer internals of [ParticleSystem] and [FilterPoints], particle-trail and branching tricks, plus demos of the upcoming 4.2 help window, snapshots for live performance, MIDI/IO recording, and the new FFmpeg video playback.

## Mentions
- 0:56→2:03 [TextureMapForce] · passing · experiment · Example · 50% — briefly nudged among other tweaks while wiring a spinning, time-driven particle intro animation.
- 2:06→6:00 [ParticleSystem] [TurbulenceForce] [SnapToAnglesForce] · explained · discussion · Gotcha · 80% — freshly emitted particles default to an emit velocity of 1 that drifts them toward the camera, and while almost all forces add velocity even to fully static particles, a few like angle-snapping only act on existing motion.
- 8:35→13:20 [DrawMesh] [TextureMapForce] [ui:SymbolLibrary] · explained · discussion · Concept · 70% — the upcoming help panel shows the hovered or selected operator's documentation plus AI-extracted meetup-video references you can open as timestamped deep links, and markdown links in descriptions jump to the operator in the symbol library.
- 13:21→18:05 [ui:VariationWindow] [ui:Settings] · discussion · discussion · Gotcha · 75% — preview-on-hover for variations can hijack your live output mid-performance, and it's controlled by the "variation hover on preview" user setting, which now defaults off.
- 19:15→21:15 [ColorGrade] [ui:VariationWindow] · discussion · discussion · Gotcha · 70% — hover-previewing presets is harmless on cheap image ops, but if the mouse happens to rest on a preset thumbnail while you type a parameter value, the typed value isn't retained.
- 22:04→23:30 [ui:Settings] · passing · discussion · Example · 55% — restyled ahead of the 4.2 release, which surfaced style properties that weren't exposed to the theming system.
- 24:03→26:25 [ui:VariationWindow] [AdvancedFeedback] · explained · discussion · Gotcha · 75% — hover previews are at their worst with SDF shader recompiles and feedback effects, because every preview evaluation disturbs the accumulated feedback buffer.
- 28:34→30:30 [DrawPoints] · passing · answer · Gotcha · 65% — black outlines around blended particle sprites come from writing the Z buffer; turning z-write off fixes the look but forfeits depth-based effects like depth of field.
- 30:53→36:55 [ParticleSystem] [DrawPoints] [RadialPoints] · in-depth · answer · Concept · 90% — correct transparency requires drawing back-to-front because the per-pixel write-depth-or-not decision is binary, so a fading sprite must cut off somewhere; order-independent transparency exists but needs a heavy multi-render-target setup comparable to screen-space ambient occlusion.
- 36:58→39:50 [DrawPoints] · in-depth · answer · Parameters · 85% — AlphaCutoff decides when a fading sprite still writes depth: raising it toward fully opaque removes black outlines but hardens soft blurry textures, and fully transparent texture areas must keep the same RGB as the visible part or their color leaks into the blend.
- 40:28→41:30 [DrawPoints] · explained · answer · Performance · 80% — enable GenerateMipMaps on large sprite textures so small distant particles both look better and render much faster, since fetched texture samples end up closer together in memory.
- 41:33→49:15 [SortPoints] · in-depth · discussion · Gotcha · 85% — sorts a particle buffer by camera distance for correct transparency but currently needs a power-of-two buffer size, stays cheap up to ~100k points while millions cost real performance, and two differently-drawn particle systems can't be sorted against each other unless merged into one buffer (e.g. texture atlas plus a per-point style attribute).
- 49:20→54:35 [DrawPoints] [SortPoints] [ui:SearchWindow] · explained · discussion · Performance · 75% — plan is a sort-by-depth checkbox (off by default for speed) that swaps in the sorted buffer; the "children" search scope reveals how deep nested sub-operators go, and every embedded sub-op adds invalidation overhead, which is why hot drawing ops get rewritten in C#.
- 56:21→58:15 [Layer2d] · explained · discussion · Example · 80% — once was a sprawling operator graph computing fit-width/fit-height/cover scaling modes; rewriting the logic in C# to output a single transform matrix made it both simpler and faster, a template for other complicated internals.
- 58:21→1:00:00 [ParticleSystem] · passing · answer · Gotcha · 70% — chaining one system as the emit source of another works, but an oversized upstream MaxParticleCount means the second system tries to emit the whole buffer, not just the visible handful.
- 1:00:41→1:01:47 [TurbulenceForce] · explained · answer · Parameters · 70% — keeps pushing at constant strength while drag tapers particle speed, so turbulence appears to grow dominant; some forces offer a use-particle-velocity-as-strength option and this one could gain it too.
- 1:01:47→1:04:55 [ParticleSystem] [MapPointAttributes] · explained · answer · Parameters · 80% — trail length from chained systems is the ratio of the second buffer to the first (1000 upstream into 5000 gives 5-point trails), and attribute mapping for grow-and-fade over lifetime keeps working on the re-emitted particles.
- 1:05:04→1:10:15 [FilterPoints] [IntDiv] [CountInt] [Modulo] · explained · answer · Tip · 80% — get longer trails from fewer points by inserting a filter between two particle systems: divide MaxParticleCount for the step count, advance the start index with a frame counter, and use the built-in modulo input to wrap; splitting an existing connection line by clicking it inserts the math op inline.
- 1:10:16→1:11:45 [ui:Graph] · explained · answer · Tip · 85% — press Shift+B to bypass an operator in place instead of disconnecting it, which behaves like breaking the connection but is instantly reversible.
- 1:11:45→1:13:30 [FilterPoints] [IntToFloat] · explained · answer · Gotcha · 70% — the Step input is a float, so an integer division result must pass through an int-to-float conversion before it will connect; step then takes every Nth source point.
- 1:14:40→1:17:00 [ui:SymbolBrowser] · explained · answer · Tip · 85% — click once on an operator's output (or in the empty space below it) to get context-aware suggestions ranked by how frequently that connection occurs; placing downstream ops below rather than beside keeps the graph auto-grouped, which suits post-processing stacks.
- 1:17:00→1:20:05 [ParticleSystem] [RadialPoints] [ui:VariationWindow] · explained · experiment · Example · 75% — insert a particle system directly onto an existing connection by clicking the wire; fireworks-style bursts come from secondary emission with a small emit velocity (~5) plus the "Z to outside" radial preset picked from the presets window.
- 1:20:10→1:21:50 [CountInt] · explained · answer · Concept · 80% — increments by itself every rendered frame with nothing plugged in, and tries to detect the update cycle so loops or multiple graph evaluations per frame don't double-count.
- 1:21:55→1:23:35 [ParticleSystem] · passing · experiment · Example · 60% — feeding filtered emit points into per-particle secondary systems yields mushroom-like mini bursts, each tweakable with its own forces and damping.
- 1:23:44→1:29:50 [ParticleSystem] [Sketch] · in-depth · answer · Concept · 90% — internally a fixed-size ring buffer where unused slots are NaN "separator" points that drawing ops skip; the emit cursor wraps around overwriting the oldest particles, the emit count can change freely per frame, and multiple simultaneous emits are distributed across the buffer rather than written as one chunk.
- 1:29:51→1:32:00 [LineTextPoints] [ParticleSystem] · explained · experiment · Tip · 80% — set MaxParticleCount to -1 to size the buffer exactly to the incoming point count, then stop emitting: the buffer becomes persistent state you can push around with forces without lifetimes expiring.
- 1:32:03→1:33:25 [ReconstructiveForce] · explained · experiment · Example · 85% — constantly bleeds the original emit positions back into the simulation, so turbulence-scattered particles periodically re-form their source shape.
- 1:33:28→1:41:50 [FilterPoints] · in-depth · answer · Concept · 90% — Count sets the output buffer length and it always tries to fill it; each output cell runs in parallel in a compute shader and infers its source index purely from its own index via start index plus step, wrapping around the source buffer; Step is deliberately a float to allow staggered/grouped Euclidean-style distributions, and Scatter adds per-cell randomization.
- 1:42:26→1:43:55 [RenderTarget] [CountInt] · explained · experiment · Tip · 80% — test a randomness or distribution hypothesis by rendering into a target without clearing it: accumulated hits converge to constant brightness if the distribution is uniform, exposing gaps a single frame would hide.
- 1:44:05→1:46:50 [FilterPoints] [TransformPoints] · explained · experiment · Example · 70% — random raindrop-style emission from a predefined point set: a negative count keeps the last buffer size, scatter picks random source points per frame, and swapping the seed for a frame counter on start index turns randomness into a sequential diagonal sweep.
- 1:48:04→1:49:30 [OscInput] · passing · discussion · Example · 55% — an OSC-triggered audio-visual set shown briefly as context for the following particle-branching question.
- 1:51:03→1:53:35 [PerlinNoise] [ParticleSystem] [FilterPoints] [CountInt] · explained · experiment · Example · 70% — for branch trails, drive a single emit point with noise, then use a one-frame boolean event to advance a counter feeding the filter's start index, so each beat picks the most recently emitted particle.
- 1:53:40→1:56:40 [ParticleSystem] · explained · discussion · Gotcha · 80% — lifetime -1 auto-infers lifetime from emit rate versus buffer length (one per frame into ten slots means ten frames), which silently breaks with sporadic event-driven emission — set an explicit lifetime in that case.
- 1:56:41→1:59:00 [PairPointsForLines] [TurbulenceForce] [DrawLines] · explained · experiment · Example · 65% — pairing two point buffers draws connecting lines between them, and a particle system with no forces and zero emit velocity works as a plain storage buffer holding the connection targets.
- 2:02:30→2:05:25 [Trigger] [CountInt] · explained · experiment · Tip · 70% — counters that started on random frames drift out of phase with each other; wire a single trigger into every counter's Reset input to re-align them all at once.
- 2:05:26→2:08:15 [ParticleSystem] · explained · discussion · Concept · 70% — the internal write/emit index exists but isn't exposed; surfacing the last-emitted index as an output would let you address the newest particle and compute offsets relative to it for branching effects.
- 2:09:55→2:11:35 [ParticleSystem] [CustomPointShader] · explained · discussion · Tip · 75% — track the current emission point by emitting the same source point into a tiny second system whose max count of 1 always holds exactly the newest position; a custom shader could then fade the paired connection lines more gracefully.
- 2:12:00→2:14:45 [SortPoints] · explained · discussion · Concept · 70% — future direction: sort by arbitrary float keys (f1/f2 or a parallel key buffer) written by custom shader passes, so any property — size, distance to an object — can drive draw order, not just camera distance.
- 2:15:00→2:19:55 [SortPoints] [FirstValidTexture] · explained · discussion · Parameters · 70% — the mandatory camera-reference input can be dropped by reading the context transform buffer internally; a "first valid" fallback picker (like the existing one for textures) would use the reference when connected and the active camera otherwise, without spamming warnings.
- 2:20:00→2:26:55 [ReuseCamera] [OrbitCamera] [GridPoints] [VisualizePoints] · explained · experiment · Example · 60% — verifying that sorted indices re-order while orbiting the camera; the reuse-camera pattern for feeding an existing camera's transform into consumers is acknowledged as awkward and slated for simplification.
- 2:27:00→2:35:00 [ui:Graph] · explained · discussion · Gotcha · 80% — dragging several connection lines at once can rewire inputs into the wrong slots (a buffer landing on a UAV input) because cached connection targets aren't invalidated for multi-line drags; reconnecting the wires one at a time restores a working graph.
- 2:37:44→2:39:20 [SortPoints] [DrawBillboards] · explained · discussion · Comparison · 75% — distance-to-camera-center and distance-to-camera-plane are both legitimate sort keys: center matters when rendering cube maps, plane matters for screen-aligned billboards, which is why billboards carry separate orientation settings for each.
- 2:45:05→2:49:10 [AddInts] [IntAdd] [IntDiv] · explained · discussion · Gotcha · 75% — duplicate integer-add operators get resolved by convention: the plural multi-input form ([AddInts]) survives, [IntAdd] is flagged obsolete rather than deleted because operators are referenced by ID, and renames keep old projects loading.
- 2:56:20→3:01:15 [RaymarchField] · explained · discussion · Concept · 60% — the marching loop has odd-looking code but behaves correctly, and a demoscene "overshoot then trace back" precision trick was tried by two people independently with barely any visible improvement, especially on complex fields.
- 3:01:20→3:12:10 [CustomSDF] [ui:Field] · in-depth · discussion · Gotcha · 85% — can't currently return color or material because its user function only receives the position, unlike the internal getField call whose fourth parameter switches between distance and material evaluation; the proposed fix passes that flag through so a color pass runs only at final hit positions, which is roughly two orders of magnitude cheaper than marching again.
- 3:15:50→3:17:35 [TorusMesh] [DrawMesh] [OrbitCamera] [RenderTarget] [ui:Symbol] · passing · discussion · Example · 65% — turn a scratch patch into its own reusable project op by selecting the nodes and combining them into a new symbol.
- 3:17:35→3:18:45 [ui:VariationWindow] · explained · discussion · Concept · 80% — presets apply saved parameter sets to the selected operator, and holding Alt while picking blends between two presets instead of hard-switching.
- 3:18:46→3:23:10 [ui:VariationWindow] [ui:ParameterWindow] · in-depth · discussion · Concept · 85% — snapshots capture parameters across all operators in a graph: opt each parameter in (marked green), save whole looks, and click the empty graph background to get a summary panel of every snapshot-controlled parameter — an Ableton-macro-like control surface for live sets.
- 3:23:15→3:26:30 [ui:Annotations] [LoadObj] [DrawMeshUnlit] [CountInt] · explained · discussion · Concept · 65% — planned: group the snapshot parameter summary by graph annotations ("post effects", "scene 1") into collapsible stacks so hundred-op live setups stay navigable.
- 3:26:30→3:29:55 [ui:VariationWindow] · explained · discussion · Tip · 75% — snapshots can be crossfaded and several mixed simultaneously for transitions; deleting an operator silently drops its values from existing snapshots, and thumbnails regenerate in the background or on demand.
- 3:30:00→3:32:10 [MidiInput] [ui:Settings] · explained · answer · Tip · 80% — after plugging in a controller, rescan devices in settings; the first physical slider movement teaches the mapping to the connected parameter, then adjust the output range to fit.
- 3:32:12→3:39:05 [MidiRecording] · in-depth · discussion · Concept · 85% — records all incoming IO (MIDI, OSC, audio events) into data clips that can be played back to simulate a live performer, so you can tweak mappings and looks offline without them playing non-stop; dragging in a MIDI file to convert into the same format is planned as a small addition.
- 3:39:05→3:44:00 [MidiRecording] [ui:TimeOverrides] · explained · discussion · Gotcha · 65% — the recording is a binary-searchable lookup table of timed events (pure data, fast to query), but beware that Ableton exports MIDI files fixed at 120 BPM regardless of project tempo, so synced instruments drift unless compensated.
- 3:45:21→3:49:35 [ProjectLight] [DrawMeshWithShadow] [SSAO] · explained · answer · Comparison · 75% — current shadow options: a projector-style light with halfway-decent single-source shadows, directional draw-with-shadow, and a point-light variant using percentage-closer filtering for slightly blurred edges; ambient occlusion softens shading but is no substitute for real shadows.
- 3:49:40→3:51:45 [ui:Symbol] · passing · discussion · Gotcha · 55% — entering and leaving a read-only built-in operator prompts to save changes even when nothing was edited (seen on Linux under 4.1.1), acknowledged as a bug to fix.
- 3:56:50→4:01:50 [PlayVideo] [ui:Timeline] [PerlinNoise] [Remap] · in-depth · discussion · Parameters · 80% — the new FFmpeg engine lets you drag a video onto the timeline and generate a ProRes proxy for smooth scrubbing; OverrideTimeInSeconds (fed e.g. by remapped noise) enables procedural frame skipping, and two playback modes trade off: Fast Seeking caches frames CPU-side for bidirectional scrubbing, while Playback Performance uses GPU H264/HEVC decode straight into a texture.
