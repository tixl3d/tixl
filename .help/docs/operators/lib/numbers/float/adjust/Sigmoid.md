# Sigmoid

*in [Lib.numbers.float.adjust](README.md)*

Returns a sigmoid function 1/(1+e^x-1).
It's very similar to [SmoothStep] but never reaches the -1 and 1 boundaries.

## Input Parameters
| Name (Relevancy & Type) | Description |
|---|---|
| **Value** (Single) | — |
| **Stretch** (Single) | — |

## Outputs
| Name | Type |
|---|---|
| **Result** | System.Single |

