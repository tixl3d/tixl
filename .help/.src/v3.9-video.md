Hi there! we've just released Tooll version 3.9. If you're new here and wondering what all this is about: Tooll 3 is an open-source application for generating motion graphics. It can be used for animation, live performances, and installations. It's slick, easy to use, yet incredibly powerful.

In the 10 weeks since the release of v3.8, the community has added a vast number of new features and improvements. You can download the latest version (of course, for free) and read all the details on the website. You can find the link below.

In this video, I'll walk you through the highlights! So, let's dive right in!

Let's start with documentation.

More and more people are discovering Tooll3. Although we design everything with usability in mind, many users might never discover some of Tooll's powerful features due to a lack of documentation. Improving the situation was a key focus of the new release: we extended documentation to operator parameters. And because the help page for most operators is now much more extensive, it's displayed when hovering over the help icon. Clicking that icon will toggle the help mode.

We also cleaned up the design of the parameter window and aligned the parameter order of many ops. To keep things more consistent, we wrote an extensive style guide for documentation and operator consistency. A huge shout out to all the contributors working on this effort. The result really shows!

Additionally, we started a Wiki page called "Realtime rendering for artists". It covers many technical questions most artist will encounter when diving into realtime content. You can find the link in the description. If you still have questions, join us on Discord or leave comment below. We'll answer all of them and add them to the page.


Now, let's look at some new features, shall we?

Video Exporting:

Exporting videos is now up to 10 times faster. Not only that, but you no longer need to convert the format for rendering. Additionally, we implemented audio export. This means you can render your audio-reactive projects with super high quality and motion blur.

- **increment version number** checkbox. Now, you can keep the render window docked. And repeatedly hit "Start Render" to save out intermediate work steps.

GLTF

glTF is becoming the de facto file format for exporting 3D file assets. Tooll now supports not only loading single meshes, but complete scenes and we automatically load and combine textures into correct channel maps for Physically Based Rendering (PBR).

Here is an example:

- We can use the symbol browser to create the [LoadGltfScene] operator.
- We can click the "Edit scene setup" to adjust the different notes.
- Here, we also see the textures for each material.
- The documentation mentions the [DrawScene] operator. When we drag this onto the graph and connect it, we can see the scene in all its beauty.
- The operator also provides some parameters to override the file's roughness and metallic settings to adjust the appearance.

To combine gltf-objects with Tooll's mesh effects pipeline, the operator also provides outputs for a Mesh and a Material:

Let's draw instances of our mesh:
- Connect [DrawAtPoints] to the mesh output.
- Connect a [UseMaterial] to the material.

This is just the first set of features. Over the next versions, we will implement more cool glTF features like animations, skinning, blend shapes, and camera definitions.

----

You can now use the [AbletonLinkSync] operator to connect to any application in your network that sends Ableton-Link™ synchronization. Because Ableton made this protocol available as a free standard, there is now a wide range of applications that support this feature. Besides Ableton Live, there are Traktor, BitWig, Touch Designer, VVVV, and many more. Additionally a lot of DJ hardware can also support that protocol.

This is huge, because AbletonLink not only sends the tempo (I.e. BPM rate like with MIDI) but also the syncing within bars. And because all timing in Tooll3 is using bars, everything (I mean everything) now is syncing perfectly: Connect a [AnimValue] to a [Blob.Radius] -> voila: perfect sync. No more tapping at VJ acts!

Point style
------------

One of Tooll's central data-types are points. After feedback from the community we've completely overhauled points to support colors and stretching.
We added many operators to set, modify or randomize colors and scaling. Of of course there are infinite ways to combine these with the hundreds of already existing point operators. This opens up the road for even more flexible content generation. Here are three quick examples:

In the first one, we generate some RadialPoints, apply some Noise and use these points to draw some mesh instances.

We can now insert the RandomizePoints operator to add some colors or scaling to our instances. RandomizePoints is awesome, because its random seed can be a float value which results in very nice deterministic animations.

In the next example I'm using SamplePointColors on a video texture and combined it with [DrawBillboards]. This one also has a RandomPhase parameter -- Along with a lot of other parameters that might finally make sense because they now have some documentation. Better late than never :D

In the last example we combine the result of two [LinePoint]s. For one we set the start and and colors. For the other we use the [SpreadPointAttributes] operator to slide a color gradient. Because we didn't adjust the point count of the repeated lines, we can apply an additional noise offset and use the cool new infinity slider to crank up the Amount parameter.

When playing around with operators and their presets, you'll find many cool other combinations to set and use point colors. And I really encourage you to post them on our share your stuff thread on discord.

## Bias and Gain + Vector2 Controls

Next up is the new combined bias and gain parameter. It might sound a like toy for math geeks. But you'll soon see why we really fell in love with this new control.

So what can we actually do with that? Let's have a look at the [BiasAndGainExample] operator.

It shows the bias and gain mapping curve as a yellow line. It works like an adjustment curve in a traditional image manipulation software: Mapping blacks in the lower left corner and whites in the upper right. A straight line has no effect. Changing the curve to an S shape will increase the contrast. The reverse will reduce contrast, eventually leaving a gray image.

The S-shaped function is called gain. It concentrates either at the center or at the edges.

The alternative shape is called bias: it pushes the values either towards to upper or or lower edges, making an image brighter or darker, respectively.

The incredible thing about the BiasAndGain parameter is that it combines both of these shapes. Each of these examples uses the same bias and gain parameters but it controls totally different aspects: from images..., to distributions... to easing curve for animations -- bias and gain is a single parameter to rule them all.

In fact, this feature is soo flexible, that it would be almost unusable without a cool new control that we added to the parameter window. You can enable it by opening the parameter settings and enable the Vec2 control. Now you can simultaneously dragging both parameters see a visual representation of the settings through distribution curves and weights. Cool, eh?

We've already incorporated BiasAndGain into [Remap], [RandomizePoints], [FractalNoise], [RemapColors], [Tint], [LinePoints], [RadialPoints], [PerlinNoise], [TextureMapForce], and numerous other operators.

---

Here are some more improvements to Tooll's user interface that are coming with v3.9:

Working with parameters:

- You can now rename input parameters from the Editor.
- The Beat tapping interaction has been improved with added keyboard shortcuts Z (tap) and X (resync measure start).
- UI scale and display scale settings have been improved for better compatibility with Windows' display scale settings like 125%.
- Formatting presets have been added to float and vector parameters for quick optimization of display, scale, and ranges.
 - We improved the layout of the parameter window and made grouping easier to scan. Also, long parameter names are no longer cut off if there is enough width.

- Gradient manipulation now supports undo/redo.
- Evenly distributing gradient steps with Hold interpolation now also shows the last step.
- In focus mode switching between foreground and background control by clicking on the right edge  is now easier.
- Midi-Devices can be rescanned from the settings window without restarting Tooll.
- Duplicating a symbol now keeps dropdown definitions.
- A new *Multiply Alpha* blend mode has been added to most image operators for which allows powerful masking.

-------------
Let's take a look at some operators that we've added or updated:

We've drastically improved the performance of NdiInput, which now works great with all kinds of input formats, sources, and can support 4k footage.


AdvancedFeedback2 is a great addition to AdvancedFeedback. It comes with a wide range of presets and can create very nice-looking fluid-like effects.

Dither is a cool effect that adds a retro look to your design. It's a good example of how we always try to implement effects with the widest range of possibilities using the least amount of controllable parameters. It comes with nice documentation and presets, and obviously, we just had to add bias gain controls. Like most image operators, it also features the new list of blend modes.

Also nice are the new Noise variants: Worley and Shard. We've also updated and cleaned up FractalNoise.

Tint and its bigger brother RemapColors are two color adjustment operators that really benefit from the bias and gain. RemapColors now supports color cycling, which can be great for generating effects.

----------------
Now, Let's look at some of the updates to Particle Effects...

Let's start with a recap: As shown in the "how to use particles" tutorial, the ParticleSystem op is where the magic happens. We add some emit points, add a force..., render the output as points, and there we have it: a simple particle system.

So let's talk about the changes:

We've rethought how lifetime is handled: Its default setting of -1 tells the system to use the maximum lifetime calculated from the number of emitted points and the available buffer length. When writing the particle age to the "w" attribute, this value is now in a range between 0 and 1. We can use this attribute to color our particles over the lifetime...

...or scale instanced geometry with this value. The [DrawMeshAtPoints2] operator provides many options to do that.

I briefly want to mention two new forces we've added to the particle system: Both let you control the particles in interesting ways.

- The first one, [TextureBasedForce], accelerates particles along the connected normal map.
- So we connect the required [Normals] input. The documentation explains that we need to use normals with the signed normal mode.
- Let's create an input for our NormalMap by rendering [Text] into a [RenderTarget] and applying [Blur].
- When we start playing around with the parameters or the presets, we quickly find really interesting results.

The "Twist" parameter twists the angle from the normal map. If you increase that to 180°, the normal map direction if reverse. The "confine depth" parameter tries to contain all particles within a given visible distance from the camera.

We can use this setup to highlight the other force I was mentioning: [SnapToAnglesForce]. What a funky name... This force slightly twists the velocity of particles towards the defined angles which can produce really nice grid structures.

---

Looking at the roadmap, we already have big plans for the upcoming versions. We're currently working on a new system how you can organize your projects and resources. This will take some work, but its an important step on the way to towards support for vulcan rendering so Tooll can finally be used on Linux and Mac.

And for version 11 we'll be finally integrating the new magnetic graph system that we shared as a preview last year. It will make working with tooll even snappier and live patching even more fun.

I want to close by sharing some highlights from our community.

You don't need to be a c# programming crack to support the project. You can also help us by testing, improving documentation, coming up with good ideas for new features or with spreading the word by sharing this video. So join us on discord.
---

Tooll 3.9 comes with too many new Operators and updates to mention in this video. Once again I recommend checking the release notes on each version.

We're super excited about our ambitious plans for upcoming versions. Presently, we're focused on developing a new system for organizing projects and resources. This represents a significant refactor, but it's a crucial step towards supporting Vulcan rendering, enabling Tooll to be compatible with Linux and Mac operating systems.

Furthermore, version 11 will see the long-awaited integration of the new magnetic graph system, which we previewed last year. This addition will enhance the intuitive and enjoyable experience of working with Tooll and make live patching even more fun.

As you can see, there's much on our plate. We really appreciate any support we receive. You don't have to be a C# programming expert to contribute to the project. Whether it's through testing, enhancing documentation, suggesting new features, or spreading the word by sharing this video, your participation is invaluable. Join us on Discord and be part of our community.





___________________

https://www.heise.de/kontakt/

Liebe ct-Redaktion

Das ist eine Anwendung zur Erstellung von Motion-Grafiken, Animationen und Videoeffekten. Vor Kurzem haben wir die Version 3.9 veröffentlicht. Diese beiden Videos geben einen kurzen Überblick über die Software:

Die Anwendung basiert im Kern auf dem .NET Framework und realtime-Rendering mit DirectX und erlaubt Live-Coding mit Hot-Code-Reloading. Diese Mächtigkeit wird kombiniert mit einem intuitiven Grafik-Interface, das mit Presets, Parametern und Keyframes auch Künstler und Grafiker anspricht. Tooll schlägt so erfolgreich eine Brücke zwischen Design und Code.

Nach mehreren tausend Commits haben wir inzwischen eine kleine, aber sehr engagierte, internationale Community auf Discord und 3000 Sterne auf GitHub erreicht. Das Projekt läuft stabil und hat eine umfangreiche Dokumentation mit vielen Tutorials und Beispielen.

Leider läuft Tooll momentan nur unter Windows, aber wir arbeiten an einer Portierung des Grafik-Layers zu Vulkan, um auch eine Unterstützung für Mac und Linux zu ermöglichen.

Im Moment arbeiten zwei professionelle Entwickler unentgeltlich an Tooll3. Die Liste der Features auf der Roadmap ist sehr lang. Einige Aufgaben sind komplex, wie die Vulkan-Portierung und die Integration eines NuGet-Package-Systems, andere sind einfach nur "Arbeit", wie zum Beispiel ein vernünftiger Installer oder die Anbindung weiterer video Formate.

Es wäre großartig, wenn langfristig mit Tooll eine Open-Source-Alternative zu Anwendungen wie Adobe After Effects entstehen könnte. Ich kann mir vorstellen, dass ein kurzer Artikel bei heise-News oder sogar in der c't etwas mehr Aufmerksamkeit auf das Projekt lenken würde. Im Moment suchen wir insbesondere Entwickler mit C#- und Computergrafik-Hintergrund. Natürlich helfen auch aktive Anwender dabei, das Projekt durch Feedback, Tutorials oder Beispiele voranzutreiben.

Bei interesse stehe jederzeit gerne für Rückfragen oder eine kurze Demo zur Verfügung.


liebe Grüße,
Tom (pixtur)


_______________


- https://www.creativebloq.com/contact
  - https://www.creativebloq.com/author/joseph-foley
  - 
- https://motionographer.com/contact/
  - https://motionographer.com/submit/
- https://www.provideocoalition.com/contact/
- https://vjun.io/
- https://nofilmschool.com/contact

- https://www.blendernation.com/contact/
    - 
- https://resolume.com/contact
- https://www.cineversity.com/contact
- 
- https://www.digitalartsonline.co.uk/contact/
- https://www.fxguide.com/contact/
    - https://www.fxguide.com/contact/


- https://www.creativeapplications.net/submit-project/
- https://cdm.link/contact/
- https://www.prmtr.org/contact
- 








